generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "mysql"
}

model Role {
  id                            BigInt           @id @default(autoincrement()) @db.UnsignedBigInt
  name                          String           @db.VarChar(100)
  slug                          String           @unique(map: "slug") @db.VarChar(100)
  description                   String?          @db.VarChar(255)
  createdAt                     DateTime         @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                     DateTime         @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  is_active                     Boolean          @default(true)
  created_by                    BigInt?          @db.UnsignedBigInt
  updated_by                    BigInt?          @db.UnsignedBigInt
  rolePermissions               RolePermission[]
  users_roles_created_byTousers User?            @relation("roles_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_roles_created_by")
  users_roles_updated_byTousers User?            @relation("roles_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_roles_updated_by")
  users                         User[]

  @@index([created_by], map: "fk_roles_created_by")
  @@index([updated_by], map: "fk_roles_updated_by")
  @@map("roles")
}

model Permission {
  id                                  BigInt           @id @default(autoincrement()) @db.UnsignedBigInt
  name                                String           @db.VarChar(150)
  slug                                String           @unique(map: "slug") @db.VarChar(150)
  module                              String           @db.VarChar(100)
  createdAt                           DateTime         @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                           DateTime         @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  is_active                           Boolean          @default(true)
  created_by                          BigInt?          @db.UnsignedBigInt
  updated_by                          BigInt?          @db.UnsignedBigInt
  users_permissions_created_byTousers User?            @relation("permissions_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_permissions_created_by")
  users_permissions_updated_byTousers User?            @relation("permissions_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_permissions_updated_by")
  rolePermissions                     RolePermission[]

  @@index([created_by], map: "fk_permissions_created_by")
  @@index([updated_by], map: "fk_permissions_updated_by")
  @@map("permissions")
}

model RolePermission {
  id                                       BigInt     @id @default(autoincrement()) @db.UnsignedBigInt
  roleId                                   BigInt     @map("role_id") @db.UnsignedBigInt
  permissionId                             BigInt     @map("permission_id") @db.UnsignedBigInt
  createdAt                                DateTime   @default(now()) @map("created_at") @db.Timestamp(0)
  updated_at                               DateTime   @default(now()) @db.Timestamp(0)
  is_active                                Boolean    @default(true)
  created_by                               BigInt?    @db.UnsignedBigInt
  updated_by                               BigInt?    @db.UnsignedBigInt
  users_role_permissions_created_byTousers User?      @relation("role_permissions_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_role_permissions_created_by")
  users_role_permissions_updated_byTousers User?      @relation("role_permissions_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_role_permissions_updated_by")
  permission                               Permission @relation(fields: [permissionId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_rp_permission")
  role                                     Role       @relation(fields: [roleId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_rp_role")

  @@unique([roleId, permissionId], map: "uq_role_permission")
  @@index([created_by], map: "fk_role_permissions_created_by")
  @@index([updated_by], map: "fk_role_permissions_updated_by")
  @@index([permissionId], map: "fk_rp_permission")
  @@map("role_permissions")
}

model User {
  id                                                                  BigInt                     @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                                                String?                    @db.VarChar(125)
  roleId                                                              BigInt                     @map("role_id") @db.UnsignedBigInt
  cust_id                                                             String?                    @db.VarChar(20)
  name                                                                String                     @db.VarChar(150)
  email                                                               String?                    @unique(map: "email") @db.VarChar(150)
  phone                                                               String?                    @unique(map: "phone") @db.VarChar(20)
  password_hash                                                       String?                    @db.VarChar(255)
  avatar                                                              String?                    @db.VarChar(500)
  status                                                              users_status               @default(active)
  email_verified_at                                                   DateTime?                  @db.Timestamp(0)
  phone_verified_at                                                   DateTime?                  @db.Timestamp(0)
  last_login_at                                                       DateTime?                  @db.Timestamp(0)
  createdAt                                                           DateTime                   @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                                                           DateTime                   @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  deleted_at                                                          DateTime?                  @db.Timestamp(0)
  is_active                                                           Boolean                    @default(true)
  created_by                                                          BigInt?                    @db.UnsignedBigInt
  updated_by                                                          BigInt?                    @db.UnsignedBigInt
  is_blocked                                                          BigInt?
  blocked_at                                                          DateTime?                  @default(now()) @db.Timestamp(0)
  attribute_values_attribute_values_created_byTousers                 AttributeValue[]           @relation("attribute_values_created_byTousers")
  attribute_values_attribute_values_updated_byTousers                 AttributeValue[]           @relation("attribute_values_updated_byTousers")
  audit_logs_audit_logs_created_byTousers                             AuditLog[]                 @relation("audit_logs_created_byTousers")
  audit_logs_audit_logs_updated_byTousers                             AuditLog[]                 @relation("audit_logs_updated_byTousers")
  auditLogs                                                           AuditLog[]
  banner_positions_banner_positions_created_byTousers                 banner_positions[]         @relation("banner_positions_created_byTousers")
  banner_positions_banner_positions_updated_byTousers                 banner_positions[]         @relation("banner_positions_updated_byTousers")
  banners_banners_created_byTousers                                   Banner[]                   @relation("banners_created_byTousers")
  banners_banners_updated_byTousers                                   Banner[]                   @relation("banners_updated_byTousers")
  blog_categories_blog_categories_created_byTousers                   blog_categories[]          @relation("blog_categories_created_byTousers")
  blog_categories_blog_categories_updated_byTousers                   blog_categories[]          @relation("blog_categories_updated_byTousers")
  blog_comments_blog_comments_created_byTousers                       blog_comments[]            @relation("blog_comments_created_byTousers")
  blog_comments_blog_comments_updated_byTousers                       blog_comments[]            @relation("blog_comments_updated_byTousers")
  blog_comments_blog_comments_user_idTousers                          blog_comments[]            @relation("blog_comments_user_idTousers")
  blogs                                                               Blog[]
  blogs_blogs_created_byTousers                                       Blog[]                     @relation("blogs_created_byTousers")
  blogs_blogs_updated_byTousers                                       Blog[]                     @relation("blogs_updated_byTousers")
  cart_items_cart_items_created_byTousers                             CartItem[]                 @relation("cart_items_created_byTousers")
  cart_items_cart_items_updated_byTousers                             CartItem[]                 @relation("cart_items_updated_byTousers")
  carts                                                               Cart[]
  carts_carts_created_byTousers                                       Cart[]                     @relation("carts_created_byTousers")
  carts_carts_updated_byTousers                                       Cart[]                     @relation("carts_updated_byTousers")
  combo_product_items_combo_product_items_created_byTousers           combo_product_items[]      @relation("combo_product_items_created_byTousers")
  combo_product_items_combo_product_items_updated_byTousers           combo_product_items[]      @relation("combo_product_items_updated_byTousers")
  combo_products_combo_products_created_byTousers                     combo_products[]           @relation("combo_products_created_byTousers")
  combo_products_combo_products_updated_byTousers                     combo_products[]           @relation("combo_products_updated_byTousers")
  companies_companies_created_byTousers                               Company[]                  @relation("companies_created_byTousers")
  companies_companies_updated_byTousers                               Company[]                  @relation("companies_updated_byTousers")
  bulk_order_enquiries_bulk_order_enquiries_created_byTousers         bulk_order_enquiries[]     @relation("bulk_order_enquiries_created_byTousers")
  bulk_order_enquiries_bulk_order_enquiries_updated_byTousers         bulk_order_enquiries[]     @relation("bulk_order_enquiries_updated_byTousers")
  contact_messages_contact_messages_created_byTousers                 contact_messages[]         @relation("contact_messages_created_byTousers")
  contact_messages_contact_messages_updated_byTousers                 contact_messages[]         @relation("contact_messages_updated_byTousers")
  coupon_usage_coupon_usage_created_byTousers                         coupon_usage[]             @relation("coupon_usage_created_byTousers")
  coupon_usage_coupon_usage_updated_byTousers                         coupon_usage[]             @relation("coupon_usage_updated_byTousers")
  coupon_usage_coupon_usage_user_idTousers                            coupon_usage[]             @relation("coupon_usage_user_idTousers")
  coupons_coupons_created_byTousers                                   Coupon[]                   @relation("coupons_created_byTousers")
  coupons_coupons_updated_byTousers                                   Coupon[]                   @relation("coupons_updated_byTousers")
  addresses                                                           CustomerAddress[]
  customer_addresses_customer_addresses_created_byTousers             CustomerAddress[]          @relation("customer_addresses_created_byTousers")
  customer_addresses_customer_addresses_updated_byTousers             CustomerAddress[]          @relation("customer_addresses_updated_byTousers")
  customer_profiles_customer_profiles_referred_byTousers              customer_profiles[]        @relation("customer_profiles_referred_byTousers")
  customer_profiles_customer_profiles_user_idTousers                  customer_profiles?         @relation("customer_profiles_user_idTousers")
  customer_profiles_customer_profiles_created_byTousers               customer_profiles[]        @relation("customer_profiles_created_byTousers")
  customer_profiles_customer_profiles_updated_byTousers               customer_profiles[]        @relation("customer_profiles_updated_byTousers")
  customer_reports_customer_reports_created_byTousers                 customer_reports[]         @relation("customer_reports_created_byTousers")
  customer_reports_customer_reports_updated_byTousers                 customer_reports[]         @relation("customer_reports_updated_byTousers")
  delivery_partners_delivery_partners_created_byTousers               delivery_partners[]        @relation("delivery_partners_created_byTousers")
  delivery_partners_delivery_partners_updated_byTousers               delivery_partners[]        @relation("delivery_partners_updated_byTousers")
  delivery_slots_delivery_slots_created_byTousers                     delivery_slots[]           @relation("delivery_slots_created_byTousers")
  delivery_slots_delivery_slots_updated_byTousers                     delivery_slots[]           @relation("delivery_slots_updated_byTousers")
  faq_faq_created_byTousers                                           faq[]                      @relation("faq_created_byTousers")
  faq_faq_updated_byTousers                                           faq[]                      @relation("faq_updated_byTousers")
  inventories_inventories_created_byTousers                           Inventory[]                @relation("inventories_created_byTousers")
  inventories_inventories_updated_byTousers                           Inventory[]                @relation("inventories_updated_byTousers")
  inventory_transactions_inventory_transactions_updated_byTousers     InventoryTransaction[]     @relation("inventory_transactions_updated_byTousers")
  inventory_transactions_inventory_transactions_created_byTousers     InventoryTransaction[]     @relation("inventory_transactions_created_byTousers")
  login_logs_login_logs_created_byTousers                             login_logs[]               @relation("login_logs_created_byTousers")
  login_logs_login_logs_updated_byTousers                             login_logs[]               @relation("login_logs_updated_byTousers")
  login_logs_login_logs_user_idTousers                                login_logs[]               @relation("login_logs_user_idTousers")
  newsletter_subscribers_newsletter_subscribers_created_byTousers     newsletter_subscribers[]   @relation("newsletter_subscribers_created_byTousers")
  newsletter_subscribers_newsletter_subscribers_updated_byTousers     newsletter_subscribers[]   @relation("newsletter_subscribers_updated_byTousers")
  notification_logs_notification_logs_created_byTousers               notification_logs[]        @relation("notification_logs_created_byTousers")
  notification_logs_notification_logs_updated_byTousers               notification_logs[]        @relation("notification_logs_updated_byTousers")
  notification_templates_notification_templates_created_byTousers     notification_templates[]   @relation("notification_templates_created_byTousers")
  notification_templates_notification_templates_updated_byTousers     notification_templates[]   @relation("notification_templates_updated_byTousers")
  notifications                                                       Notification[]
  notifications_notifications_created_byTousers                       Notification[]             @relation("notifications_created_byTousers")
  notifications_notifications_updated_byTousers                       Notification[]             @relation("notifications_updated_byTousers")
  offer_products_offer_products_created_byTousers                     offer_products[]           @relation("offer_products_created_byTousers")
  offer_products_offer_products_updated_byTousers                     offer_products[]           @relation("offer_products_updated_byTousers")
  offer_items_offer_items_created_byTousers                           offer_items[]              @relation("offer_items_created_byTousers")
  offer_items_offer_items_updated_byTousers                           offer_items[]              @relation("offer_items_updated_byTousers")
  offers_offers_created_byTousers                                     Offer[]                    @relation("offers_created_byTousers")
  offers_offers_updated_byTousers                                     Offer[]                    @relation("offers_updated_byTousers")
  order_addresses_order_addresses_created_byTousers                   OrderAddress[]             @relation("order_addresses_created_byTousers")
  order_addresses_order_addresses_updated_byTousers                   OrderAddress[]             @relation("order_addresses_updated_byTousers")
  order_items_order_items_created_byTousers                           OrderItem[]                @relation("order_items_created_byTousers")
  order_items_order_items_updated_byTousers                           OrderItem[]                @relation("order_items_updated_byTousers")
  order_status_history_order_status_history_created_byTousers         order_status_history[]     @relation("order_status_history_created_byTousers")
  order_status_history_order_status_history_updated_byTousers         order_status_history[]     @relation("order_status_history_updated_byTousers")
  order_status_history_order_status_history_changed_byTousers         order_status_history[]     @relation("order_status_history_changed_byTousers")
  orders                                                              Order[]
  orders_orders_created_byTousers                                     Order[]                    @relation("orders_created_byTousers")
  orders_orders_updated_byTousers                                     Order[]                    @relation("orders_updated_byTousers")
  otp_verifications_otp_verifications_user_idTousers                  otp_verifications[]        @relation("otp_verifications_user_idTousers")
  otp_verifications_otp_verifications_created_byTousers               otp_verifications[]        @relation("otp_verifications_created_byTousers")
  otp_verifications_otp_verifications_updated_byTousers               otp_verifications[]        @relation("otp_verifications_updated_byTousers")
  page_contents_page_contents_created_byTousers                       page_contents[]            @relation("page_contents_created_byTousers")
  page_contents_page_contents_updated_byTousers                       page_contents[]            @relation("page_contents_updated_byTousers")
  pages_pages_created_byTousers                                       pages[]                    @relation("pages_created_byTousers")
  pages_pages_updated_byTousers                                       pages[]                    @relation("pages_updated_byTousers")
  payment_gateway_webhooks_payment_gateway_webhooks_created_byTousers payment_gateway_webhooks[] @relation("payment_gateway_webhooks_created_byTousers")
  payment_gateway_webhooks_payment_gateway_webhooks_updated_byTousers payment_gateway_webhooks[] @relation("payment_gateway_webhooks_updated_byTousers")
  payment_methods_payment_methods_created_byTousers                   payment_methods[]          @relation("payment_methods_created_byTousers")
  payment_methods_payment_methods_updated_byTousers                   payment_methods[]          @relation("payment_methods_updated_byTousers")
  payment_transactions_payment_transactions_created_byTousers         PaymentTransaction[]       @relation("payment_transactions_created_byTousers")
  payment_transactions_payment_transactions_updated_byTousers         PaymentTransaction[]       @relation("payment_transactions_updated_byTousers")
  payments_payments_created_byTousers                                 Payment[]                  @relation("payments_created_byTousers")
  payments_payments_updated_byTousers                                 Payment[]                  @relation("payments_updated_byTousers")
  permissions_permissions_created_byTousers                           Permission[]               @relation("permissions_created_byTousers")
  permissions_permissions_updated_byTousers                           Permission[]               @relation("permissions_updated_byTousers")
  pincode_serviceability_pincode_serviceability_created_byTousers     pincode_serviceability[]   @relation("pincode_serviceability_created_byTousers")
  pincode_serviceability_pincode_serviceability_updated_byTousers     pincode_serviceability[]   @relation("pincode_serviceability_updated_byTousers")
  product_attribute_values_product_attribute_values_created_byTousers product_attribute_values[] @relation("product_attribute_values_created_byTousers")
  product_attribute_values_product_attribute_values_updated_byTousers product_attribute_values[] @relation("product_attribute_values_updated_byTousers")
  product_attributes_product_attributes_created_byTousers             ProductAttribute[]         @relation("product_attributes_created_byTousers")
  product_attributes_product_attributes_updated_byTousers             ProductAttribute[]         @relation("product_attributes_updated_byTousers")
  product_brands_product_brands_created_byTousers                     ProductBrand[]             @relation("product_brands_created_byTousers")
  product_brands_product_brands_updated_byTousers                     ProductBrand[]             @relation("product_brands_updated_byTousers")
  product_categories_product_categories_created_byTousers             ProductCategory[]          @relation("product_categories_created_byTousers")
  product_categories_product_categories_updated_byTousers             ProductCategory[]          @relation("product_categories_updated_byTousers")
  product_category_images_product_category_images_created_byTousers   product_category_images[]  @relation("product_category_images_created_byTousers")
  product_category_images_product_category_images_updated_byTousers   product_category_images[]  @relation("product_category_images_updated_byTousers")
  product_gst_rates_product_gst_rates_created_byTousers               product_gst_rates[]        @relation("product_gst_rates_created_byTousers")
  product_gst_rates_product_gst_rates_updated_byTousers               product_gst_rates[]        @relation("product_gst_rates_updated_byTousers")
  product_hsn_codes_product_hsn_codes_created_byTousers               product_hsn_codes[]        @relation("product_hsn_codes_created_byTousers")
  product_hsn_codes_product_hsn_codes_updated_byTousers               product_hsn_codes[]        @relation("product_hsn_codes_updated_byTousers")
  product_images_product_images_created_byTousers                     ProductImage[]             @relation("product_images_created_byTousers")
  product_images_product_images_updated_byTousers                     ProductImage[]             @relation("product_images_updated_byTousers")
  product_questions_product_questions_answered_byTousers              product_questions[]        @relation("product_questions_answered_byTousers")
  product_questions_product_questions_user_idTousers                  product_questions[]        @relation("product_questions_user_idTousers")
  product_questions_product_questions_created_byTousers               product_questions[]        @relation("product_questions_created_byTousers")
  product_questions_product_questions_updated_byTousers               product_questions[]        @relation("product_questions_updated_byTousers")
  product_tag_maps_product_tag_maps_created_byTousers                 product_tag_maps[]         @relation("product_tag_maps_created_byTousers")
  product_tag_maps_product_tag_maps_updated_byTousers                 product_tag_maps[]         @relation("product_tag_maps_updated_byTousers")
  product_tags_product_tags_created_byTousers                         product_tags[]             @relation("product_tags_created_byTousers")
  product_tags_product_tags_updated_byTousers                         product_tags[]             @relation("product_tags_updated_byTousers")
  product_units_product_units_created_byTousers                       product_units[]            @relation("product_units_created_byTousers")
  product_units_product_units_updated_byTousers                       product_units[]            @relation("product_units_updated_byTousers")
  product_variant_images_product_variant_images_created_byTousers     product_variant_images[]   @relation("product_variant_images_created_byTousers")
  product_variant_images_product_variant_images_updated_byTousers     product_variant_images[]   @relation("product_variant_images_updated_byTousers")
  product_variants_product_variants_created_byTousers                 ProductVariant[]           @relation("product_variants_created_byTousers")
  product_variants_product_variants_updated_byTousers                 ProductVariant[]           @relation("product_variants_updated_byTousers")
  variant_unit_prices_created_byTousers                               VariantUnitPrice[]         @relation("variant_unit_prices_created_byTousers")
  variant_unit_prices_updated_byTousers                               VariantUnitPrice[]         @relation("variant_unit_prices_updated_byTousers")
  products_products_created_byTousers                                 Product[]                  @relation("products_created_byTousers")
  products_products_updated_byTousers                                 Product[]                  @relation("products_updated_byTousers")
  produt_brand_images_produt_brand_images_created_byTousers           produt_brand_images[]      @relation("produt_brand_images_created_byTousers")
  produt_brand_images_produt_brand_images_updated_byTousers           produt_brand_images[]      @relation("produt_brand_images_updated_byTousers")
  recently_viewed_products_recently_viewed_products_created_byTousers recently_viewed_products[] @relation("recently_viewed_products_created_byTousers")
  recently_viewed_products_recently_viewed_products_updated_byTousers recently_viewed_products[] @relation("recently_viewed_products_updated_byTousers")
  recently_viewed_products_recently_viewed_products_user_idTousers    recently_viewed_products[] @relation("recently_viewed_products_user_idTousers")
  redirect_urls_redirect_urls_created_byTousers                       redirect_urls[]            @relation("redirect_urls_created_byTousers")
  redirect_urls_redirect_urls_updated_byTousers                       redirect_urls[]            @relation("redirect_urls_updated_byTousers")
  refunds_refunds_created_byTousers                                   refunds[]                  @relation("refunds_created_byTousers")
  refunds_refunds_updated_byTousers                                   refunds[]                  @relation("refunds_updated_byTousers")
  return_items_return_items_created_byTousers                         return_items[]             @relation("return_items_created_byTousers")
  return_items_return_items_updated_byTousers                         return_items[]             @relation("return_items_updated_byTousers")
  return_requests_return_requests_created_byTousers                   return_requests[]          @relation("return_requests_created_byTousers")
  return_requests_return_requests_updated_byTousers                   return_requests[]          @relation("return_requests_updated_byTousers")
  return_requests_return_requests_user_idTousers                      return_requests[]          @relation("return_requests_user_idTousers")
  review_images_review_images_created_byTousers                       ReviewImage[]              @relation("review_images_created_byTousers")
  review_images_review_images_updated_byTousers                       ReviewImage[]              @relation("review_images_updated_byTousers")
  reviews                                                             Review[]
  reviews_reviews_created_byTousers                                   Review[]                   @relation("reviews_created_byTousers")
  reviews_reviews_updated_byTousers                                   Review[]                   @relation("reviews_updated_byTousers")
  reward_points_reward_points_created_byTousers                       reward_points[]            @relation("reward_points_created_byTousers")
  reward_points_reward_points_updated_byTousers                       reward_points[]            @relation("reward_points_updated_byTousers")
  reward_points_reward_points_user_idTousers                          reward_points?             @relation("reward_points_user_idTousers")
  reward_transactions_reward_transactions_created_byTousers           reward_transactions[]      @relation("reward_transactions_created_byTousers")
  reward_transactions_reward_transactions_updated_byTousers           reward_transactions[]      @relation("reward_transactions_updated_byTousers")
  reward_transactions_reward_transactions_user_idTousers              reward_transactions[]      @relation("reward_transactions_user_idTousers")
  role_permissions_role_permissions_created_byTousers                 RolePermission[]           @relation("role_permissions_created_byTousers")
  role_permissions_role_permissions_updated_byTousers                 RolePermission[]           @relation("role_permissions_updated_byTousers")
  roles_roles_created_byTousers                                       Role[]                     @relation("roles_created_byTousers")
  roles_roles_updated_byTousers                                       Role[]                     @relation("roles_updated_byTousers")
  sales_reports_sales_reports_created_byTousers                       sales_reports[]            @relation("sales_reports_created_byTousers")
  sales_reports_sales_reports_updated_byTousers                       sales_reports[]            @relation("sales_reports_updated_byTousers")
  search_history_search_history_created_byTousers                     search_history[]           @relation("search_history_created_byTousers")
  search_history_search_history_updated_byTousers                     search_history[]           @relation("search_history_updated_byTousers")
  search_history_search_history_user_idTousers                        search_history[]           @relation("search_history_user_idTousers")
  seo_meta_seo_meta_created_byTousers                                 seo_meta[]                 @relation("seo_meta_created_byTousers")
  seo_meta_seo_meta_updated_byTousers                                 seo_meta[]                 @relation("seo_meta_updated_byTousers")
  settings_settings_created_byTousers                                 settings[]                 @relation("settings_created_byTousers")
  settings_settings_updated_byTousers                                 settings[]                 @relation("settings_updated_byTousers")
  shipment_tracking_shipment_tracking_created_byTousers               shipment_tracking[]        @relation("shipment_tracking_created_byTousers")
  shipment_tracking_shipment_tracking_updated_byTousers               shipment_tracking[]        @relation("shipment_tracking_updated_byTousers")
  shipments_shipments_created_byTousers                               shipments[]                @relation("shipments_created_byTousers")
  shipments_shipments_updated_byTousers                               shipments[]                @relation("shipments_updated_byTousers")
  shipments_shipments_delivery_staffTousers                           shipments[]                @relation("shipments_delivery_staffTousers")
  shipping_charges_shipping_charges_created_byTousers                 shipping_charges[]         @relation("shipping_charges_created_byTousers")
  shipping_charges_shipping_charges_updated_byTousers                 shipping_charges[]         @relation("shipping_charges_updated_byTousers")
  shipping_zones_shipping_zones_created_byTousers                     shipping_zones[]           @relation("shipping_zones_created_byTousers")
  shipping_zones_shipping_zones_updated_byTousers                     shipping_zones[]           @relation("shipping_zones_updated_byTousers")
  stock_adjustments_stock_adjustments_created_byTousers               stock_adjustments[]        @relation("stock_adjustments_created_byTousers")
  stock_adjustments_stock_adjustments_updated_byTousers               stock_adjustments[]        @relation("stock_adjustments_updated_byTousers")
  stock_adjustments_stock_adjustments_adjusted_byTousers              stock_adjustments[]        @relation("stock_adjustments_adjusted_byTousers")
  stock_reports_stock_reports_created_byTousers                       stock_reports[]            @relation("stock_reports_created_byTousers")
  stock_reports_stock_reports_updated_byTousers                       stock_reports[]            @relation("stock_reports_updated_byTousers")
  user_sessions_user_sessions_user_idTousers                          user_sessions[]            @relation("user_sessions_user_idTousers")
  user_sessions_user_sessions_created_byTousers                       user_sessions[]            @relation("user_sessions_created_byTousers")
  user_sessions_user_sessions_updated_byTousers                       user_sessions[]            @relation("user_sessions_updated_byTousers")
  users_users_created_byTousers                                       User?                      @relation("users_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_users_created_by")
  other_users_users_created_byTousers                                 User[]                     @relation("users_created_byTousers")
  role                                                                Role                       @relation(fields: [roleId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_users_role")
  users_users_updated_byTousers                                       User?                      @relation("users_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_users_updated_by")
  other_users_users_updated_byTousers                                 User[]                     @relation("users_updated_byTousers")
  wallet_transactions_wallet_transactions_created_byTousers           WalletTransaction[]        @relation("wallet_transactions_created_byTousers")
  wallet_transactions_wallet_transactions_updated_byTousers           WalletTransaction[]        @relation("wallet_transactions_updated_byTousers")
  wallet                                                              Wallet?
  wallets_wallets_created_byTousers                                   Wallet[]                   @relation("wallets_created_byTousers")
  wallets_wallets_updated_byTousers                                   Wallet[]                   @relation("wallets_updated_byTousers")
  wishlist_items_wishlist_items_created_byTousers                     WishlistItem[]             @relation("wishlist_items_created_byTousers")
  wishlist_items_wishlist_items_updated_byTousers                     WishlistItem[]             @relation("wishlist_items_updated_byTousers")
  wishlistItems                                                       WishlistItem[]
  users_variant_price_history_created_byTousers                       variant_price_history[]    @relation("variant_price_history_created_byTousers")
  users_variant_price_history_updated_byTousers                       variant_price_history[]    @relation("variant_price_history_updated_byTousers")

  @@index([created_by], map: "fk_users_created_by")
  @@index([roleId], map: "fk_users_role")
  @@index([updated_by], map: "fk_users_updated_by")
  @@index([status], map: "idx_users_status")
  @@map("users")
}

model ProductCategory {
  id                                         BigInt                    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                       String?                   @default("UUID()") @db.VarChar(255)
  parentId                                   BigInt?                   @map("parent_id") @db.UnsignedBigInt
  name                                       String                    @db.VarChar(150)
  slug                                       String                    @unique(map: "slug") @db.VarChar(170)
  description                                String?                   @db.Text
  icon                                       String?                   @db.VarChar(500)
  status                                     Boolean?                  @default(true)
  isActive                                   Boolean                   @default(true) @map("is_active")
  sortOrder                                  Int                       @default(0) @map("sort_order")
  createdAt                                  DateTime                  @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                                  DateTime                  @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  deleted_at                                 DateTime?                 @db.Timestamp(0)
  created_by                                 BigInt?                   @db.UnsignedBigInt
  updated_by                                 BigInt?                   @db.UnsignedBigInt
  parent                                     ProductCategory?          @relation("CategoryTree", fields: [parentId], references: [id], onUpdate: NoAction, map: "fk_cat_parent")
  children                                   ProductCategory[]         @relation("CategoryTree")
  users_product_categories_created_byTousers User?                     @relation("product_categories_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_categories_created_by")
  users_product_categories_updated_byTousers User?                     @relation("product_categories_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_categories_updated_by")
  product_category_images                    product_category_images[]
  category_attributes                        category_attributes[]

  @@index([parentId], map: "fk_cat_parent")
  @@index([created_by], map: "fk_categories_created_by")
  @@index([updated_by], map: "fk_categories_updated_by")
  @@index([slug], map: "idx_cat_slug")
  @@map("product_categories")
}

model ProductBrand {
  id                                     BigInt                @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                   String?               @default("UUID()") @db.VarChar(255)
  name                                   String                @db.VarChar(150)
  slug                                   String                @unique(map: "slug") @db.VarChar(170)
  description                            String?               @db.Text
  status                                 Boolean               @default(true)
  isActive                               Boolean               @default(true) @map("is_active")
  createdAt                              DateTime              @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                              DateTime              @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  deleted_at                             DateTime?             @db.Timestamp(0)
  created_by                             BigInt?               @db.UnsignedBigInt
  updated_by                             BigInt?               @db.UnsignedBigInt
  users_product_brands_created_byTousers User?                 @relation("product_brands_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_brands_created_by")
  users_product_brands_updated_byTousers User?                 @relation("product_brands_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_brands_updated_by")
  products                               Product[]
  produt_brand_images                    produt_brand_images[]

  @@index([created_by], map: "fk_brands_created_by")
  @@index([updated_by], map: "fk_brands_updated_by")
  @@map("product_brands")
}

model Product {
  id                               BigInt                     @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                             String?                    @unique(map: "idx_products_uuid") @default("UUID()") @db.VarChar(255)
  categoryId                       BigInt?                    @map("category_id")
  brandId                          BigInt?                    @map("brand_id") @db.UnsignedBigInt
  hsn_code_id                      BigInt?                    @db.UnsignedBigInt
  name                             String                     @db.VarChar(200)
  slug                             String                     @unique(map: "slug") @db.VarChar(220)
  sku                              String?                    @unique(map: "sku") @db.VarChar(100)
  base_price                       Decimal                    @default(0.00) @db.Decimal(10, 2)
  sale_price                       Decimal?                   @db.Decimal(10, 2)
  status                           Boolean?                   @default(true)
  isActive                         Boolean                    @default(true) @map("is_active")
  createdAt                        DateTime                   @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                        DateTime                   @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  deleted_at                       DateTime?                  @db.Timestamp(0)
  created_by                       BigInt?                    @db.UnsignedBigInt
  updated_by                       BigInt?                    @db.UnsignedBigInt
  cartItems                        CartItem[]
  combo_product_items              combo_product_items[]
  offer_products                   offer_products[]
  orderItems                       OrderItem[]
  product_attribute_values         product_attribute_values[]
  images                           ProductImage[]
  product_questions                product_questions[]
  product_tag_maps                 product_tag_maps[]
  variants                         ProductVariant[]
  brand                            ProductBrand?              @relation(fields: [brandId], references: [id], onUpdate: NoAction, map: "fk_products_brand")
  users_products_created_byTousers User?                      @relation("products_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_products_created_by")
  product_hsn_codes                product_hsn_codes?         @relation(fields: [hsn_code_id], references: [id], onUpdate: NoAction, map: "fk_products_hsn")
  users_products_updated_byTousers User?                      @relation("products_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_products_updated_by")
  recently_viewed_products         recently_viewed_products[]
  reviews                          Review[]
  wishlistItems                    WishlistItem[]

  @@index([brandId], map: "fk_products_brand")
  @@index([created_by], map: "fk_products_created_by")
  @@index([hsn_code_id], map: "fk_products_hsn")
  @@index([updated_by], map: "fk_products_updated_by")
  @@index([isActive], map: "idx_products_active")
  @@index([slug], map: "idx_products_slug")
  @@index([categoryId], map: "idx_products_category")
  @@map("products")
}

model ProductVariant {
  id                                       BigInt                   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                     String                   @default("UUID()") @db.VarChar(255)
  productId                                BigInt                   @map("product_id") @db.UnsignedBigInt
  variant_name                             String                   @default("") @map("variant_name") @db.VarChar(100)
  slug                                     String?                  @unique(map: "idx_variant_slug") @db.VarChar(255)
  short_description                        String?                  @db.VarChar(500)
  description                              String?                  @db.Text
  ingredients                              String?                  @db.Text
  is_ready_to_mix                          Boolean                  @default(false) @map("is_ready_to_mix")
  cooking_recipe                           String?                  @map("cooking_recipe") @db.Text
  shelf_life                               String?                  @map("shelf_life") @db.VarChar(100)
  veg_type                                 products_veg_type        @default(na)
  color_name                               String?                  @map("color_name") @db.VarChar(50)
  color_hex                                String?                  @map("color_hex") @db.VarChar(7)
  is_featured                              Boolean                  @default(false)
  is_default                               Boolean                  @default(false)
  isActive                                 Boolean                  @default(true) @map("is_active")
  out_of_stock                             Boolean                  @default(false) @map("out_of_stock")
  createdAt                                DateTime                 @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                                DateTime                 @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  deleted_at                               DateTime?                @db.Timestamp(0)
  created_by                               BigInt?                  @db.UnsignedBigInt
  updated_by                               BigInt?                  @db.UnsignedBigInt
  product_variant_images                   product_variant_images[]
  users_product_variants_created_byTousers User?                    @relation("product_variants_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_product_variants_created_by")
  users_product_variants_updated_byTousers User?                    @relation("product_variants_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_product_variants_updated_by")
  product                                  Product                  @relation(fields: [productId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_variant_product")
  variant_unit_prices                      VariantUnitPrice[]
  cart_items                               CartItem[]
  wishlist_items                           WishlistItem[]
  order_items                              OrderItem[]

  @@index([created_by], map: "fk_product_variants_created_by")
  @@index([updated_by], map: "fk_product_variants_updated_by")
  @@index([productId], map: "idx_variant_product")
  @@map("product_variants")
}

model VariantUnitPrice {
  id                                          BigInt                  @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                        String                  @default("UUID()") @db.VarChar(255)
  variant_id                                  BigInt                  @map("variant_id") @db.UnsignedBigInt
  unit_id                                     BigInt                  @map("unit_id") @db.UnsignedBigInt
  unit_value                                  Decimal                 @default(0.00) @db.Decimal(10, 2)
  sku                                         String                  @unique(map: "sku") @db.VarChar(100)
  base_price                                  Decimal                 @default(0.00) @db.Decimal(10, 2)
  is_default                                  Boolean                 @default(false)
  isActive                                    Boolean                 @default(true) @map("is_active")
  createdAt                                   DateTime                @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                                   DateTime                @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  deleted_at                                  DateTime?               @db.Timestamp(0)
  created_by                                  BigInt?                 @db.UnsignedBigInt
  updated_by                                  BigInt?                 @db.UnsignedBigInt
  variant                                     ProductVariant          @relation(fields: [variant_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_vup_variant")
  product_units                               product_units           @relation(fields: [unit_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_vup_unit")
  inventories                                 Inventory?
  inventory_transactions                      InventoryTransaction[]
  variant_price_history                       variant_price_history[]
  users_variant_unit_prices_created_byTousers User?                   @relation("variant_unit_prices_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_vup_created_by")
  users_variant_unit_prices_updated_byTousers User?                   @relation("variant_unit_prices_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_vup_updated_by")
  cartItems                                   CartItem[]
  orderItems                                  OrderItem[]
  wishlist_items                              WishlistItem[]
  reviews                                     Review[]
  combo_product_items                         combo_product_items[]
  stock_adjustments                           stock_adjustments[]
  stock_reports                               stock_reports[]
  offer_items                                 offer_items[]

  @@index([variant_id], map: "fk_vup_variant")
  @@index([unit_id], map: "fk_vup_unit")
  @@index([created_by], map: "fk_vup_created_by")
  @@index([updated_by], map: "fk_vup_updated_by")
  @@map("variant_unit_prices")
}

model ProductImage {
  id                                     BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  productId                              BigInt   @map("product_id") @db.UnsignedBigInt
  image_url                              String   @db.VarChar(500)
  altText                                String?  @map("alt_text") @db.VarChar(150)
  isPrimary                              Boolean  @default(false) @map("is_primary")
  sortOrder                              Int      @default(0) @map("sort_order")
  createdAt                              DateTime @default(now()) @map("created_at") @db.Timestamp(0)
  updated_at                             DateTime @default(now()) @db.Timestamp(0)
  is_active                              Boolean  @default(true)
  created_by                             BigInt?  @db.UnsignedBigInt
  updated_by                             BigInt?  @db.UnsignedBigInt
  product                                Product  @relation(fields: [productId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_pimg_product")
  users_product_images_created_byTousers User?    @relation("product_images_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_product_images_created_by")
  users_product_images_updated_byTousers User?    @relation("product_images_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_product_images_updated_by")

  @@index([productId], map: "fk_pimg_product")
  @@index([created_by], map: "fk_product_images_created_by")
  @@index([updated_by], map: "fk_product_images_updated_by")
  @@map("product_images")
}

model ProductAttribute {
  id                                         BigInt                     @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                       String?                    @default("UUID()") @db.VarChar(255)
  name                                       String                     @db.VarChar(100)
  slug                                       String                     @unique(map: "slug") @db.VarChar(120)
  createdAt                                  DateTime                   @default(now()) @map("created_at") @db.Timestamp(0)
  updated_at                                 DateTime                   @default(now()) @db.Timestamp(0)
  is_active                                  Boolean                    @default(true)
  created_by                                 BigInt?                    @db.UnsignedBigInt
  updated_by                                 BigInt?                    @db.UnsignedBigInt
  values                                     AttributeValue[]
  product_attribute_values                   product_attribute_values[]
  category_attributes                        category_attributes[]
  users_product_attributes_created_byTousers User?                      @relation("product_attributes_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_product_attributes_created_by")
  users_product_attributes_updated_byTousers User?                      @relation("product_attributes_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_product_attributes_updated_by")

  @@index([created_by], map: "fk_product_attributes_created_by")
  @@index([updated_by], map: "fk_product_attributes_updated_by")
  @@map("product_attributes")
}

model AttributeValue {
  id                                       BigInt                     @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                     String?                    @default("UUID()") @db.VarChar(255)
  attributeId                              BigInt                     @map("attribute_id") @db.UnsignedBigInt
  value                                    String                     @db.VarChar(150)
  createdAt                                DateTime                   @default(now()) @map("created_at") @db.Timestamp(0)
  updated_at                               DateTime                   @default(now()) @db.Timestamp(0)
  is_active                                Boolean                    @default(true)
  created_by                               BigInt?                    @db.UnsignedBigInt
  updated_by                               BigInt?                    @db.UnsignedBigInt
  users_attribute_values_created_byTousers User?                      @relation("attribute_values_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_attribute_values_created_by")
  users_attribute_values_updated_byTousers User?                      @relation("attribute_values_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_attribute_values_updated_by")
  attribute                                ProductAttribute           @relation(fields: [attributeId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_attrval_attr")
  product_attribute_values                 product_attribute_values[]

  @@index([created_by], map: "fk_attribute_values_created_by")
  @@index([updated_by], map: "fk_attribute_values_updated_by")
  @@index([attributeId], map: "fk_attrval_attr")
  @@map("attribute_values")
}

/// Links a ProductAttribute to the categories it applies to (e.g. "Fabric" -> Dresses,
/// "Movement" -> Watches), so the admin product form only prompts for attributes that
/// are relevant to the product's category instead of one fixed field set for everything.
model category_attributes {
  id                 BigInt           @id @default(autoincrement()) @db.UnsignedBigInt
  category_id        BigInt           @db.UnsignedBigInt
  attribute_id       BigInt           @db.UnsignedBigInt
  is_required        Boolean          @default(false)
  sort_order         Int              @default(0)
  created_at         DateTime         @default(now()) @db.Timestamp(0)
  updated_at         DateTime         @default(now()) @db.Timestamp(0)
  product_categories ProductCategory  @relation(fields: [category_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_catattr_category")
  product_attributes ProductAttribute @relation(fields: [attribute_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_catattr_attribute")

  @@unique([category_id, attribute_id], map: "uniq_category_attribute")
  @@index([category_id], map: "fk_catattr_category")
  @@index([attribute_id], map: "fk_catattr_attribute")
}

model Cart {
  id                            BigInt       @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                          String?      @default("UUID()") @db.VarChar(255)
  userId                        BigInt?      @map("user_id") @db.UnsignedBigInt
  sessionId                     String?      @map("session_id") @db.VarChar(150)
  coupon_id                     BigInt?      @db.UnsignedBigInt
  status                        carts_status @default(active)
  last_activity_at              DateTime?    @db.Timestamp(0)
  createdAt                     DateTime     @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                     DateTime     @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  is_active                     Boolean      @default(true)
  created_by                    BigInt?      @db.UnsignedBigInt
  updated_by                    BigInt?      @db.UnsignedBigInt
  items                         CartItem[]
  coupons                       Coupon?      @relation(fields: [coupon_id], references: [id], onUpdate: NoAction, map: "fk_cart_coupon")
  user                          User?        @relation(fields: [userId], references: [id], onUpdate: NoAction, map: "fk_cart_user")
  users_carts_created_byTousers User?        @relation("carts_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_carts_created_by")
  users_carts_updated_byTousers User?        @relation("carts_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_carts_updated_by")
  orders                        Order[]

  @@index([coupon_id], map: "fk_cart_coupon")
  @@index([userId], map: "fk_cart_user")
  @@index([created_by], map: "fk_carts_created_by")
  @@index([updated_by], map: "fk_carts_updated_by")
  @@index([sessionId], map: "idx_cart_session")
  @@index([status], map: "idx_cart_status")
  @@map("carts")
}

model CartItem {
  id                                 BigInt            @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                               String?           @default("UUID()") @db.VarChar(255)
  cartId                             BigInt            @map("cart_id") @db.UnsignedBigInt
  productId                          BigInt            @map("product_id") @db.UnsignedBigInt
  variantId                          BigInt            @map("variant_id") @db.UnsignedBigInt
  variantUnitPriceId                 BigInt?           @map("variant_unit_price_id") @db.UnsignedBigInt
  quantity                           Int               @default(1)
  price_at_add                       Decimal           @db.Decimal(10, 2)
  createdAt                          DateTime          @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                          DateTime          @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  is_active                          Boolean           @default(true)
  created_by                         BigInt?           @db.UnsignedBigInt
  updated_by                         BigInt?           @db.UnsignedBigInt
  users_cart_items_created_byTousers User?             @relation("cart_items_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_cart_items_created_by")
  users_cart_items_updated_byTousers User?             @relation("cart_items_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_cart_items_updated_by")
  cart                               Cart              @relation(fields: [cartId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_cartitem_cart")
  product                            Product           @relation(fields: [productId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_cartitem_product")
  variant                            ProductVariant    @relation(fields: [variantId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_cartitem_variant")
  variant_unit_price                 VariantUnitPrice? @relation(fields: [variantUnitPriceId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_cartitem_variant_unit_price")

  @@unique([cartId, variantId, variantUnitPriceId], map: "uq_cartitem_cart_variant_unitprice")
  @@index([created_by], map: "fk_cart_items_created_by")
  @@index([updated_by], map: "fk_cart_items_updated_by")
  @@index([cartId], map: "fk_cartitem_cart")
  @@index([productId], map: "fk_cartitem_product")
  @@index([variantId], map: "fk_cartitem_variant")
  @@index([variantUnitPriceId], map: "fk_cartitem_variant_unit_price")
  @@map("cart_items")
}

model WishlistItem {
  id                                     BigInt            @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                   String?           @default("UUID()") @db.VarChar(255)
  userId                                 BigInt            @map("user_id") @db.UnsignedBigInt
  productId                              BigInt            @map("product_id") @db.UnsignedBigInt
  variantId                              BigInt?           @map("variant_id") @db.UnsignedBigInt
  variant_unit_price_id                  BigInt?           @db.UnsignedBigInt
  createdAt                              DateTime          @default(now()) @map("created_at") @db.Timestamp(0)
  updated_at                             DateTime          @default(now()) @db.Timestamp(0)
  is_active                              Boolean           @default(true)
  created_by                             BigInt?           @db.UnsignedBigInt
  updated_by                             BigInt?           @db.UnsignedBigInt
  users_wishlist_items_created_byTousers User?             @relation("wishlist_items_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_wishlist_items_created_by")
  users_wishlist_items_updated_byTousers User?             @relation("wishlist_items_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_wishlist_items_updated_by")
  product                                Product           @relation(fields: [productId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_wishlist_product")
  user                                   User              @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_wishlist_user")
  variant                                ProductVariant?   @relation(fields: [variantId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_wishlist_variant")
  variant_unit_price                     VariantUnitPrice? @relation(fields: [variant_unit_price_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_wishlist_variant_unit_price")

  @@unique([userId, productId, variantId], map: "uq_wishlist_user_product_variant")
  @@index([created_by], map: "fk_wishlist_items_created_by")
  @@index([updated_by], map: "fk_wishlist_items_updated_by")
  @@index([productId], map: "fk_wishlist_product")
  @@index([variantId], map: "fk_wishlist_variant")
  @@index([variant_unit_price_id], map: "fk_wishlist_variant_unit_price")
  @@map("wishlist_items")
}

model Order {
  id                             BigInt                 @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                           String?                @default("UUID()") @db.VarChar(255)
  orderNumber                    String                 @unique(map: "order_number") @map("order_number") @db.VarChar(50)
  userId                         BigInt                 @map("user_id") @db.UnsignedBigInt
  cart_id                        BigInt?                @db.UnsignedBigInt
  couponId                       BigInt?                @map("coupon_id") @db.UnsignedBigInt
  delivery_slot_id               BigInt?                @db.UnsignedBigInt
  order_status                   orders_order_status    @default(pending)
  payment_status                 orders_payment_status  @default(pending)
  subtotal                       Decimal                @default(0.00) @db.Decimal(10, 2)
  discountAmount                 Decimal                @default(0.00) @map("discount_amount") @db.Decimal(10, 2)
  taxAmount                      Decimal                @default(0.00) @map("tax_amount") @db.Decimal(10, 2)
  shipping_charge                Decimal                @default(0.00) @db.Decimal(10, 2)
  totalAmount                    Decimal                @default(0.00) @map("total_amount") @db.Decimal(10, 2)
  notes                          String?                @db.VarChar(500)
  placed_at                      DateTime?              @db.Timestamp(0)
  createdAt                      DateTime               @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                      DateTime               @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  is_active                      Boolean                @default(true)
  created_by                     BigInt?                @db.UnsignedBigInt
  updated_by                     BigInt?                @db.UnsignedBigInt
  coupon_usage                   coupon_usage[]
  address                        OrderAddress[]
  items                          OrderItem[]
  order_status_history           order_status_history[]
  carts                          Cart?                  @relation(fields: [cart_id], references: [id], onUpdate: NoAction, map: "fk_order_cart")
  coupons                        Coupon?                @relation(fields: [couponId], references: [id], onUpdate: NoAction, map: "fk_order_coupon")
  delivery_slots                 delivery_slots?        @relation(fields: [delivery_slot_id], references: [id], onUpdate: NoAction, map: "fk_order_slot")
  user                           User                   @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_order_user")
  users_orders_created_byTousers User?                  @relation("orders_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_orders_created_by")
  users_orders_updated_byTousers User?                  @relation("orders_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_orders_updated_by")
  payments                       Payment[]
  refunds                        refunds[]
  return_requests                return_requests[]
  shipments                      shipments[]

  @@index([cart_id], map: "fk_order_cart")
  @@index([couponId], map: "fk_order_coupon")
  @@index([delivery_slot_id], map: "fk_order_slot")
  @@index([created_by], map: "fk_orders_created_by")
  @@index([updated_by], map: "fk_orders_updated_by")
  @@index([createdAt], map: "idx_order_created")
  @@index([order_status], map: "idx_order_status")
  @@index([userId], map: "idx_order_user")
  @@map("orders")
}

model OrderItem {
  id                                  BigInt            @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                String?           @default("UUID()") @db.VarChar(255)
  orderId                             BigInt            @map("order_id") @db.UnsignedBigInt
  productId                           BigInt            @map("product_id") @db.UnsignedBigInt
  variantId                           BigInt            @map("variant_id") @db.UnsignedBigInt
  variantUnitPriceId                  BigInt?           @map("variant_unit_price_id") @db.UnsignedBigInt
  product_name_snapshot               String            @db.VarChar(200)
  variant_snapshot                    String            @db.VarChar(100)
  sku_snapshot                        String            @db.VarChar(100)
  quantity                            Int
  unit_price                          Decimal           @db.Decimal(10, 2)
  discount_amount                     Decimal           @default(0.00) @db.Decimal(10, 2)
  tax_amount                          Decimal           @default(0.00) @db.Decimal(10, 2)
  total_price                         Decimal           @db.Decimal(10, 2)
  createdAt                           DateTime          @default(now()) @map("created_at") @db.Timestamp(0)
  updated_at                          DateTime          @default(now()) @db.Timestamp(0)
  is_active                           Boolean           @default(true)
  created_by                          BigInt?           @db.UnsignedBigInt
  updated_by                          BigInt?           @db.UnsignedBigInt
  users_order_items_created_byTousers User?             @relation("order_items_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_order_items_created_by")
  users_order_items_updated_byTousers User?             @relation("order_items_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_order_items_updated_by")
  order                               Order             @relation(fields: [orderId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_orderitem_order")
  product                             Product           @relation(fields: [productId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_orderitem_product")
  variant                             ProductVariant    @relation(fields: [variantId], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_orderitem_variant")
  variant_unit_price                  VariantUnitPrice? @relation(fields: [variantUnitPriceId], references: [id], onDelete: Restrict, onUpdate: NoAction, map: "fk_orderitem_variant_unit_price")
  return_items                        return_items[]
  reviews                             Review[]

  @@index([created_by], map: "fk_order_items_created_by")
  @@index([updated_by], map: "fk_order_items_updated_by")
  @@index([orderId], map: "fk_orderitem_order")
  @@index([productId], map: "fk_orderitem_product")
  @@index([variantId], map: "fk_orderitem_variant")
  @@index([variantUnitPriceId], map: "fk_orderitem_variant_unit_price")
  @@map("order_items")
}

model OrderAddress {
  id                                      BigInt               @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                    String?              @default("UUID()") @db.VarChar(255)
  orderId                                 BigInt               @map("order_id") @db.UnsignedBigInt
  type                                    order_addresses_type
  full_name                               String               @db.VarChar(150)
  phone                                   String               @db.VarChar(20)
  address_line1                           String               @db.VarChar(255)
  address_line2                           String?              @db.VarChar(255)
  landmark                                String?              @db.VarChar(150)
  city                                    String               @db.VarChar(100)
  state                                   String               @db.VarChar(100)
  pincode                                 String               @db.VarChar(10)
  country                                 String               @default("India") @db.VarChar(100)
  latitude                                Decimal?             @db.Decimal(10, 7)
  longitude                               Decimal?             @db.Decimal(10, 7)
  createdAt                               DateTime             @default(now()) @map("created_at") @db.Timestamp(0)
  updated_at                              DateTime             @default(now()) @db.Timestamp(0)
  is_active                               Boolean              @default(true)
  created_by                              BigInt?              @db.UnsignedBigInt
  updated_by                              BigInt?              @db.UnsignedBigInt
  users_order_addresses_created_byTousers User?                @relation("order_addresses_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_order_addresses_created_by")
  users_order_addresses_updated_byTousers User?                @relation("order_addresses_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_order_addresses_updated_by")
  order                                   Order                @relation(fields: [orderId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_orderaddr_order")

  @@index([created_by], map: "fk_order_addresses_created_by")
  @@index([updated_by], map: "fk_order_addresses_updated_by")
  @@index([orderId], map: "fk_orderaddr_order")
  @@map("order_addresses")
}

model Payment {
  id                               BigInt               @id @default(autoincrement()) @db.UnsignedBigInt
  orderId                          BigInt               @map("order_id") @db.UnsignedBigInt
  payment_method_id                BigInt               @db.UnsignedBigInt
  amount                           Decimal              @db.Decimal(10, 2)
  currency                         String               @default("INR") @db.VarChar(3)
  status                           payments_status      @default(pending)
  gateway                          String?              @db.VarChar(50)
  gateway_order_id                 String?              @db.VarChar(150)
  gateway_payment_id               String?              @db.VarChar(150)
  createdAt                        DateTime             @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                        DateTime             @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  is_active                        Boolean              @default(true)
  created_by                       BigInt?              @db.UnsignedBigInt
  updated_by                       BigInt?              @db.UnsignedBigInt
  transactions                     PaymentTransaction[]
  payment_methods                  payment_methods      @relation(fields: [payment_method_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_payment_method")
  order                            Order                @relation(fields: [orderId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_payment_order")
  users_payments_created_byTousers User?                @relation("payments_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_payments_created_by")
  users_payments_updated_byTousers User?                @relation("payments_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_payments_updated_by")
  refunds                          refunds[]

  @@index([payment_method_id], map: "fk_payment_method")
  @@index([created_by], map: "fk_payments_created_by")
  @@index([updated_by], map: "fk_payments_updated_by")
  @@index([orderId], map: "idx_payment_order")
  @@map("payments")
}

model PaymentTransaction {
  id                                           BigInt                                @id @default(autoincrement()) @db.UnsignedBigInt
  paymentId                                    BigInt                                @map("payment_id") @db.UnsignedBigInt
  transaction_type                             payment_transactions_transaction_type
  amount                                       Decimal                               @db.Decimal(10, 2)
  status                                       String                                @db.VarChar(50)
  gatewayResponse                              Json?                                 @map("gateway_response")
  createdAt                                    DateTime                              @default(now()) @map("created_at") @db.Timestamp(0)
  updated_at                                   DateTime                              @default(now()) @db.Timestamp(0)
  is_active                                    Boolean                               @default(true)
  created_by                                   BigInt?                               @db.UnsignedBigInt
  updated_by                                   BigInt?                               @db.UnsignedBigInt
  users_payment_transactions_created_byTousers User?                                 @relation("payment_transactions_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_payment_transactions_created_by")
  users_payment_transactions_updated_byTousers User?                                 @relation("payment_transactions_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_payment_transactions_updated_by")
  payment                                      Payment                               @relation(fields: [paymentId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_paytx_payment")

  @@index([created_by], map: "fk_payment_transactions_created_by")
  @@index([updated_by], map: "fk_payment_transactions_updated_by")
  @@index([paymentId], map: "fk_paytx_payment")
  @@map("payment_transactions")
}

model Review {
  id                              BigInt            @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                            String?           @unique(map: "idx_reviews_uuid") @db.VarChar(255)
  productId                       BigInt            @map("product_id") @db.UnsignedBigInt
  variant_unit_price_id           BigInt?           @map("variant_unit_price_id") @db.UnsignedBigInt
  userId                          BigInt            @map("user_id") @db.UnsignedBigInt
  order_item_id                   BigInt?           @db.UnsignedBigInt
  rating                          Int               @db.UnsignedTinyInt
  title                           String?           @db.VarChar(150)
  comment                         String?           @db.Text
  isApproved                      Boolean           @default(false) @map("is_approved")
  createdAt                       DateTime          @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                       DateTime          @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  is_active                       Boolean           @default(true)
  created_by                      BigInt?           @db.UnsignedBigInt
  updated_by                      BigInt?           @db.UnsignedBigInt
  images                          ReviewImage[]
  order_items                     OrderItem?        @relation(fields: [order_item_id], references: [id], onUpdate: NoAction, map: "fk_review_orderitem")
  product                         Product           @relation(fields: [productId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_review_product")
  variant_unit_price              VariantUnitPrice? @relation(fields: [variant_unit_price_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_review_variant_unit_price")
  user                            User              @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_review_user")
  users_reviews_created_byTousers User?             @relation("reviews_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_reviews_created_by")
  users_reviews_updated_byTousers User?             @relation("reviews_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_reviews_updated_by")

  @@index([order_item_id], map: "fk_review_orderitem")
  @@index([productId], map: "fk_review_product")
  @@index([variant_unit_price_id], map: "fk_review_variant_unit_price")
  @@index([userId], map: "fk_review_user")
  @@index([created_by], map: "fk_reviews_created_by")
  @@index([updated_by], map: "fk_reviews_updated_by")
  @@map("reviews")
}

model ReviewImage {
  id                                    BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  reviewId                              BigInt   @map("review_id") @db.UnsignedBigInt
  image_url                             String   @db.VarChar(500)
  createdAt                             DateTime @default(now()) @map("created_at") @db.Timestamp(0)
  updated_at                            DateTime @default(now()) @db.Timestamp(0)
  is_active                             Boolean  @default(true)
  created_by                            BigInt?  @db.UnsignedBigInt
  updated_by                            BigInt?  @db.UnsignedBigInt
  users_review_images_created_byTousers User?    @relation("review_images_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_review_images_created_by")
  users_review_images_updated_byTousers User?    @relation("review_images_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_review_images_updated_by")
  review                                Review   @relation(fields: [reviewId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_reviewimg_review")

  @@index([created_by], map: "fk_review_images_created_by")
  @@index([updated_by], map: "fk_review_images_updated_by")
  @@index([reviewId], map: "fk_reviewimg_review")
  @@map("review_images")
}

model Coupon {
  id                              BigInt         @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                            String?        @default("UUID()") @db.VarChar(255)
  code                            String         @unique(map: "code") @db.VarChar(50)
  type                            coupons_type
  value                           Decimal        @db.Decimal(10, 2)
  minOrderAmount                  Decimal?       @default(0.00) @map("min_order_amount") @db.Decimal(10, 2)
  max_discount_amount             Decimal?       @db.Decimal(10, 2)
  usageLimit                      Int?           @map("usage_limit")
  usage_limit_per_user            Int?           @default(1)
  valid_from                      DateTime?      @db.Timestamp(0)
  valid_to                        DateTime?      @db.Timestamp(0)
  isActive                        Boolean        @default(true) @map("is_active")
  createdAt                       DateTime       @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                       DateTime       @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  created_by                      BigInt?        @db.UnsignedBigInt
  updated_by                      BigInt?        @db.UnsignedBigInt
  carts                           Cart[]
  coupon_usage                    coupon_usage[]
  users_coupons_created_byTousers User?          @relation("coupons_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_coupons_created_by")
  users_coupons_updated_byTousers User?          @relation("coupons_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_coupons_updated_by")
  orders                          Order[]

  @@index([created_by], map: "fk_coupons_created_by")
  @@index([updated_by], map: "fk_coupons_updated_by")
  @@map("coupons")
}

model Offer {
  id                             BigInt           @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                           String?          @default("UUID()") @db.VarChar(255)
  name                           String           @db.VarChar(150)
  code                           String?          @unique(map: "uq_offers_code") @db.VarChar(50)
  level                          offers_level     @default(product)
  type                           offers_type
  value                          Decimal          @default(0.00) @db.Decimal(10, 2)
  buy_quantity                   Int?
  get_quantity                   Int?
  min_quantity                   Int              @default(1)
  max_quantity                   Int?
  minOrderAmount                 Decimal?         @default(0.00) @map("min_order_amount") @db.Decimal(10, 2)
  max_discount_amount            Decimal?         @db.Decimal(10, 2)
  priority                       Int              @default(0)
  terms                          String?          @db.Text
  startsAt                       DateTime?        @map("starts_at") @db.Timestamp(0)
  ends_at                        DateTime?        @db.Timestamp(0)
  isActive                       Boolean          @default(true) @map("is_active")
  createdAt                      DateTime         @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                      DateTime         @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  deleted_at                     DateTime?        @db.Timestamp(0)
  created_by                     BigInt?          @db.UnsignedBigInt
  updated_by                     BigInt?          @db.UnsignedBigInt
  offer_products                 offer_products[]
  offer_items                    offer_items[]
  users_offers_created_byTousers User?            @relation("offers_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_offers_created_by")
  users_offers_updated_byTousers User?            @relation("offers_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_offers_updated_by")

  @@index([created_by], map: "fk_offers_created_by")
  @@index([updated_by], map: "fk_offers_updated_by")
  @@index([isActive, startsAt, ends_at], map: "idx_offers_active_window")
  @@index([level], map: "idx_offers_level")
  @@index([priority], map: "idx_offers_priority")
  @@map("offers")
}

model Inventory {
  id                                  BigInt           @id @default(autoincrement()) @db.UnsignedBigInt
  variantUnitPriceId                  BigInt           @unique(map: "variant_unit_price_id") @map("variant_unit_price_id") @db.UnsignedBigInt
  quantity_available                  Int              @default(0)
  quantity_reserved                   Int              @default(0)
  reorderLevel                        Int              @default(0) @map("reorder_level")
  warehouse_location                  String?          @db.VarChar(150)
  updatedAt                           DateTime         @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  createdAt                           DateTime         @default(now()) @map("created_at") @db.Timestamp(0)
  is_active                           Boolean          @default(true)
  created_by                          BigInt?          @db.UnsignedBigInt
  updated_by                          BigInt?          @db.UnsignedBigInt
  variant_unit_price                  VariantUnitPrice @relation(fields: [variantUnitPriceId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_inv_variant_unit_price")
  users_inventories_created_byTousers User?            @relation("inventories_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_inventories_created_by")
  users_inventories_updated_byTousers User?            @relation("inventories_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_inventories_updated_by")

  @@index([created_by], map: "fk_inventories_created_by")
  @@index([updated_by], map: "fk_inventories_updated_by")
  @@map("inventories")
}

model InventoryTransaction {
  id                                             BigInt                      @id @default(autoincrement()) @db.UnsignedBigInt
  variant_unit_price_id                          BigInt                      @db.UnsignedBigInt
  type                                           inventory_transactions_type
  quantity                                       Int
  referenceType                                  String?                     @map("reference_type") @db.VarChar(50)
  referenceId                                    BigInt?                     @map("reference_id") @db.UnsignedBigInt
  note                                           String?                     @db.VarChar(255)
  created_by                                     BigInt?                     @db.UnsignedBigInt
  createdAt                                      DateTime                    @default(now()) @map("created_at") @db.Timestamp(0)
  updated_at                                     DateTime                    @default(now()) @db.Timestamp(0)
  is_active                                      Boolean                     @default(true)
  updated_by                                     BigInt?                     @db.UnsignedBigInt
  users_inventory_transactions_updated_byTousers User?                       @relation("inventory_transactions_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_inventory_transactions_updated_by")
  users_inventory_transactions_created_byTousers User?                       @relation("inventory_transactions_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_invtx_user")
  variant_unit_price                             VariantUnitPrice            @relation(fields: [variant_unit_price_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_invtx_variant_unit_price")

  @@index([updated_by], map: "fk_inventory_transactions_updated_by")
  @@index([created_by], map: "fk_invtx_user")
  @@index([variant_unit_price_id], map: "idx_invtx_variant_unit_price")
  @@map("inventory_transactions")
}

model CustomerAddress {
  id                                         BigInt                          @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                       String?                         @default("UUID()") @db.VarChar(255)
  userId                                     BigInt                          @map("user_id") @db.UnsignedBigInt
  cust_id                                    BigInt?
  label                                      String?                         @db.VarChar(50)
  addressType                                customer_addresses_address_type @default(shipping) @map("address_type")
  full_name                                  String                          @db.VarChar(150)
  phone                                      String                          @db.VarChar(20)
  address_line1                              String                          @db.VarChar(255)
  address_line2                              String?                         @db.VarChar(255)
  landmark                                   String?                         @db.VarChar(150)
  city                                       String                          @db.VarChar(100)
  state                                      String                          @db.VarChar(100)
  pincode                                    String?                         @db.VarChar(10)
  country                                    String                          @default("India") @db.VarChar(100)
  latitude                                   Decimal?                        @db.Decimal(10, 7)
  longitude                                  Decimal?                        @db.Decimal(10, 7)
  isDefault                                  Boolean                         @default(false) @map("is_default")
  status                                     Boolean                         @default(true)
  is_active                                  Boolean                         @default(true)
  createdAt                                  DateTime                        @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                                  DateTime                        @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  deleted_at                                 DateTime?                       @db.Timestamp(0)
  created_by                                 BigInt?                         @db.UnsignedBigInt
  updated_by                                 BigInt?                         @db.UnsignedBigInt
  user                                       User                            @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_addr_user")
  users_customer_addresses_created_byTousers User?                           @relation("customer_addresses_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_customer_addresses_created_by")
  users_customer_addresses_updated_byTousers User?                           @relation("customer_addresses_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_customer_addresses_updated_by")

  @@index([userId], map: "fk_addr_user")
  @@index([created_by], map: "fk_customer_addresses_created_by")
  @@index([updated_by], map: "fk_customer_addresses_updated_by")
  @@index([pincode], map: "idx_addr_pincode")
  @@map("customer_addresses")
}

model Blog {
  id                            BigInt           @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                          String?          @default("UUID()") @db.VarChar(255)
  blog_category_id              BigInt?          @db.UnsignedBigInt
  title                         String           @db.VarChar(200)
  slug                          String           @unique(map: "slug") @db.VarChar(220)
  content                       String           @db.LongText
  featured_image                String?          @db.VarChar(500)
  authorId                      BigInt?          @map("author_id") @db.UnsignedBigInt
  is_published                  Boolean          @default(false)
  publishedAt                   DateTime?        @map("published_at") @db.Timestamp(0)
  createdAt                     DateTime         @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                     DateTime         @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  is_active                     Boolean          @default(true)
  created_by                    BigInt?          @db.UnsignedBigInt
  updated_by                    BigInt?          @db.UnsignedBigInt
  blog_comments                 blog_comments[]
  author                        User?            @relation(fields: [authorId], references: [id], onUpdate: NoAction, map: "fk_blog_author")
  blog_categories               blog_categories? @relation(fields: [blog_category_id], references: [id], onUpdate: NoAction, map: "fk_blog_category")
  users_blogs_created_byTousers User?            @relation("blogs_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_blogs_created_by")
  users_blogs_updated_byTousers User?            @relation("blogs_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_blogs_updated_by")

  @@index([authorId], map: "fk_blog_author")
  @@index([blog_category_id], map: "fk_blog_category")
  @@index([created_by], map: "fk_blogs_created_by")
  @@index([updated_by], map: "fk_blogs_updated_by")
  @@map("blogs")
}

model Banner {
  id                              BigInt             @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                            String?            @default("UUID()") @db.VarChar(255)
  banner_position_id              BigInt             @db.UnsignedBigInt
  title                           String?            @db.VarChar(150)
  image_url                       String             @db.VarChar(500)
  link_url                        String?            @db.VarChar(500)
  media_type                      banners_media_type @default(image)
  video_url                       String?            @db.VarChar(500)
  thumbnail_url                   String?            @db.VarChar(500)
  sortOrder                       Int                @default(0) @map("sort_order")
  isActive                        Boolean            @default(true) @map("is_active")
  startsAt                        DateTime?          @map("starts_at") @db.Timestamp(0)
  ends_at                         DateTime?          @db.Timestamp(0)
  createdAt                       DateTime           @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                       DateTime           @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  created_by                      BigInt?            @db.UnsignedBigInt
  updated_by                      BigInt?            @db.UnsignedBigInt
  banner_positions                banner_positions   @relation(fields: [banner_position_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_banner_position")
  users_banners_created_byTousers User?              @relation("banners_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_banners_created_by")
  users_banners_updated_byTousers User?              @relation("banners_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_banners_updated_by")

  @@index([banner_position_id], map: "fk_banner_position")
  @@index([created_by], map: "fk_banners_created_by")
  @@index([updated_by], map: "fk_banners_updated_by")
  @@map("banners")
}

model Notification {
  id                                    BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  userId                                BigInt   @map("user_id") @db.UnsignedBigInt
  title                                 String   @db.VarChar(200)
  message                               String   @db.VarChar(500)
  type                                  String?  @db.VarChar(50)
  isRead                                Boolean  @default(false) @map("is_read")
  createdAt                             DateTime @default(now()) @map("created_at") @db.Timestamp(0)
  updated_at                            DateTime @default(now()) @db.Timestamp(0)
  is_active                             Boolean  @default(true)
  created_by                            BigInt?  @db.UnsignedBigInt
  updated_by                            BigInt?  @db.UnsignedBigInt
  user                                  User     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_notif_user")
  users_notifications_created_byTousers User?    @relation("notifications_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_notifications_created_by")
  users_notifications_updated_byTousers User?    @relation("notifications_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_notifications_updated_by")

  @@index([userId], map: "fk_notif_user")
  @@index([created_by], map: "fk_notifications_created_by")
  @@index([updated_by], map: "fk_notifications_updated_by")
  @@map("notifications")
}

model AuditLog {
  id                                 BigInt            @id @default(autoincrement()) @db.UnsignedBigInt
  userId                             BigInt?           @map("user_id") @db.UnsignedBigInt
  table_name                         String            @db.VarChar(100)
  record_id                          BigInt            @db.UnsignedBigInt
  action                             audit_logs_action
  oldValues                          Json?             @map("old_values")
  newValues                          Json?             @map("new_values")
  createdAt                          DateTime          @default(now()) @map("created_at") @db.Timestamp(0)
  updated_at                         DateTime          @default(now()) @db.Timestamp(0)
  is_active                          Boolean           @default(true)
  created_by                         BigInt?           @db.UnsignedBigInt
  updated_by                         BigInt?           @db.UnsignedBigInt
  users_audit_logs_created_byTousers User?             @relation("audit_logs_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_audit_logs_created_by")
  users_audit_logs_updated_byTousers User?             @relation("audit_logs_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_audit_logs_updated_by")
  user                               User?             @relation(fields: [userId], references: [id], onUpdate: NoAction, map: "fk_auditlog_user")

  @@index([created_by], map: "fk_audit_logs_created_by")
  @@index([updated_by], map: "fk_audit_logs_updated_by")
  @@index([userId], map: "fk_auditlog_user")
  @@index([table_name, record_id], map: "idx_audit_table_record")
  @@map("audit_logs")
}

model Wallet {
  id                              BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  userId                          BigInt              @unique(map: "user_id") @map("user_id") @db.UnsignedBigInt
  balance                         Decimal             @default(0.00) @db.Decimal(10, 2)
  createdAt                       DateTime            @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                       DateTime            @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  is_active                       Boolean             @default(true)
  created_by                      BigInt?             @db.UnsignedBigInt
  updated_by                      BigInt?             @db.UnsignedBigInt
  transactions                    WalletTransaction[]
  user                            User                @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_wallet_user")
  users_wallets_created_byTousers User?               @relation("wallets_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_wallets_created_by")
  users_wallets_updated_byTousers User?               @relation("wallets_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_wallets_updated_by")

  @@index([created_by], map: "fk_wallets_created_by")
  @@index([updated_by], map: "fk_wallets_updated_by")
  @@map("wallets")
}

model WalletTransaction {
  id                                          BigInt                   @id @default(autoincrement()) @db.UnsignedBigInt
  walletId                                    BigInt                   @map("wallet_id") @db.UnsignedBigInt
  type                                        wallet_transactions_type
  amount                                      Decimal                  @db.Decimal(10, 2)
  referenceType                               String?                  @map("reference_type") @db.VarChar(50)
  referenceId                                 BigInt?                  @map("reference_id") @db.UnsignedBigInt
  note                                        String?                  @db.VarChar(255)
  createdAt                                   DateTime                 @default(now()) @map("created_at") @db.Timestamp(0)
  updated_at                                  DateTime                 @default(now()) @db.Timestamp(0)
  is_active                                   Boolean                  @default(true)
  created_by                                  BigInt?                  @db.UnsignedBigInt
  updated_by                                  BigInt?                  @db.UnsignedBigInt
  users_wallet_transactions_created_byTousers User?                    @relation("wallet_transactions_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_wallet_transactions_created_by")
  users_wallet_transactions_updated_byTousers User?                    @relation("wallet_transactions_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_wallet_transactions_updated_by")
  wallet                                      Wallet                   @relation(fields: [walletId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_wallettx_wallet")

  @@index([created_by], map: "fk_wallet_transactions_created_by")
  @@index([updated_by], map: "fk_wallet_transactions_updated_by")
  @@index([walletId], map: "fk_wallettx_wallet")
  @@map("wallet_transactions")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model banner_positions {
  id                                       BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                     String?  @default("UUID()") @db.VarChar(255)
  name                                     String   @db.VarChar(100)
  slug                                     String   @unique(map: "slug") @db.VarChar(120)
  page                                     String?  @db.VarChar(100)
  created_at                               DateTime @default(now()) @db.Timestamp(0)
  updated_at                               DateTime @default(now()) @db.Timestamp(0)
  is_active                                Boolean  @default(true)
  created_by                               BigInt?  @db.UnsignedBigInt
  updated_by                               BigInt?  @db.UnsignedBigInt
  users_banner_positions_created_byTousers User?    @relation("banner_positions_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_banner_positions_created_by")
  users_banner_positions_updated_byTousers User?    @relation("banner_positions_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_banner_positions_updated_by")
  banners                                  Banner[]

  @@index([created_by], map: "fk_banner_positions_created_by")
  @@index([updated_by], map: "fk_banner_positions_updated_by")
}

model blog_categories {
  id                                      BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                    String?  @default("UUID()") @db.VarChar(255)
  name                                    String   @db.VarChar(150)
  slug                                    String   @unique(map: "slug") @db.VarChar(170)
  created_at                              DateTime @default(now()) @db.Timestamp(0)
  updated_at                              DateTime @default(now()) @db.Timestamp(0)
  is_active                               Boolean  @default(true)
  created_by                              BigInt?  @db.UnsignedBigInt
  updated_by                              BigInt?  @db.UnsignedBigInt
  users_blog_categories_created_byTousers User?    @relation("blog_categories_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_blog_categories_created_by")
  users_blog_categories_updated_byTousers User?    @relation("blog_categories_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_blog_categories_updated_by")
  blogs                                   Blog[]

  @@index([created_by], map: "fk_blog_categories_created_by")
  @@index([updated_by], map: "fk_blog_categories_updated_by")
}

model blog_comments {
  id                                    BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                  String?  @default("UUID()") @db.VarChar(255)
  blog_id                               BigInt   @db.UnsignedBigInt
  user_id                               BigInt?  @db.UnsignedBigInt
  name                                  String?  @db.VarChar(150)
  email                                 String?  @db.VarChar(150)
  comment                               String   @db.Text
  is_approved                           Boolean  @default(false)
  created_at                            DateTime @default(now()) @db.Timestamp(0)
  updated_at                            DateTime @default(now()) @db.Timestamp(0)
  is_active                             Boolean  @default(true)
  created_by                            BigInt?  @db.UnsignedBigInt
  updated_by                            BigInt?  @db.UnsignedBigInt
  users_blog_comments_created_byTousers User?    @relation("blog_comments_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_blog_comments_created_by")
  users_blog_comments_updated_byTousers User?    @relation("blog_comments_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_blog_comments_updated_by")
  blogs                                 Blog     @relation(fields: [blog_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_blogcomment_blog")
  users_blog_comments_user_idTousers    User?    @relation("blog_comments_user_idTousers", fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_blogcomment_user")

  @@index([created_by], map: "fk_blog_comments_created_by")
  @@index([updated_by], map: "fk_blog_comments_updated_by")
  @@index([blog_id], map: "fk_blogcomment_blog")
  @@index([user_id], map: "fk_blogcomment_user")
}

model combo_product_items {
  id                                          BigInt           @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                        String?          @default("UUID()") @db.VarChar(255)
  combo_product_id                            BigInt           @db.UnsignedBigInt
  product_id                                  BigInt           @db.UnsignedBigInt
  variant_unit_price_id                       BigInt           @db.UnsignedBigInt
  quantity                                    Int              @default(1)
  created_at                                  DateTime         @default(now()) @db.Timestamp(0)
  updated_at                                  DateTime         @default(now()) @db.Timestamp(0)
  is_active                                   Boolean          @default(true)
  created_by                                  BigInt?          @db.UnsignedBigInt
  updated_by                                  BigInt?          @db.UnsignedBigInt
  users_combo_product_items_created_byTousers User?            @relation("combo_product_items_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_combo_product_items_created_by")
  users_combo_product_items_updated_byTousers User?            @relation("combo_product_items_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_combo_product_items_updated_by")
  combo_products                              combo_products   @relation(fields: [combo_product_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_comboitem_combo")
  products                                    Product          @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_comboitem_product")
  variant_unit_price                          VariantUnitPrice @relation(fields: [variant_unit_price_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_comboitem_variant_unit_price")

  @@index([created_by], map: "fk_combo_product_items_created_by")
  @@index([updated_by], map: "fk_combo_product_items_updated_by")
  @@index([combo_product_id], map: "fk_comboitem_combo")
  @@index([product_id], map: "fk_comboitem_product")
  @@index([variant_unit_price_id], map: "fk_comboitem_variant_unit_price")
}

model combo_products {
  id                                     BigInt                @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                   String?               @default("UUID()") @db.VarChar(200)
  name                                   String                @db.VarChar(200)
  slug                                   String                @unique(map: "slug") @db.VarChar(220)
  description                            String?               @db.Text
  price                                  Decimal               @db.Decimal(10, 2)
  is_active                              Boolean               @default(true)
  created_at                             DateTime              @default(now()) @db.Timestamp(0)
  updated_at                             DateTime              @default(now()) @db.Timestamp(0)
  created_by                             BigInt?               @db.UnsignedBigInt
  updated_by                             BigInt?               @db.UnsignedBigInt
  combo_product_items                    combo_product_items[]
  users_combo_products_created_byTousers User?                 @relation("combo_products_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_combo_products_created_by")
  users_combo_products_updated_byTousers User?                 @relation("combo_products_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_combo_products_updated_by")

  @@index([created_by], map: "fk_combo_products_created_by")
  @@index([updated_by], map: "fk_combo_products_updated_by")
}

model Company {
  id                                BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                              String   @unique(map: "idx_companies_uuid") @default("UUID()") @db.VarChar(255)
  companyName                       String   @map("company_name") @db.VarChar(200)
  logo                              String?  @db.VarChar(500)
  email                             String?  @db.VarChar(150)
  phone                             String?  @db.VarChar(20)
  address                           String?  @db.Text
  city                              String?  @db.VarChar(100)
  state                             String?  @db.VarChar(100)
  country                           String?  @default("India") @db.VarChar(100)
  pincode                           String?  @db.VarChar(20)
  gstNumber                         String?  @map("gst_number") @db.VarChar(50)
  panNumber                         String?  @map("pan_number") @db.VarChar(50)
  website                           String?  @db.VarChar(255)
  isActive                          Boolean  @default(true) @map("is_active")
  createdAt                         DateTime @default(now()) @map("created_at") @db.Timestamp(0)
  updatedAt                         DateTime @default(now()) @updatedAt @map("updated_at") @db.Timestamp(0)
  createdBy                         BigInt?  @map("created_by") @db.UnsignedBigInt
  updatedBy                         BigInt?  @map("updated_by") @db.UnsignedBigInt
  users_companies_created_byTousers User?    @relation("companies_created_byTousers", fields: [createdBy], references: [id], onUpdate: NoAction, map: "fk_companies_created_by")
  users_companies_updated_byTousers User?    @relation("companies_updated_byTousers", fields: [updatedBy], references: [id], onUpdate: NoAction, map: "fk_companies_updated_by")

  @@index([createdBy], map: "fk_companies_created_by")
  @@index([updatedBy], map: "fk_companies_updated_by")
  @@map("companies")
}

model bulk_order_enquiries {
  id                                           BigInt                      @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                         String                      @unique(map: "idx_bulk_order_enquiries_uuid") @default("UUID()") @db.VarChar(255)
  name                                         String                      @db.VarChar(150)
  email                                        String                      @db.VarChar(150)
  phone                                        String                      @db.VarChar(20)
  company_name                                 String?                     @db.VarChar(150)
  product_interest                             String?                     @db.VarChar(255)
  quantity                                     Int
  message                                      String?                     @db.Text
  admin_comment                                String?                     @db.Text
  status                                       bulk_order_enquiries_status @default(new)
  created_at                                   DateTime                    @default(now()) @db.Timestamp(0)
  updated_at                                   DateTime                    @default(now()) @updatedAt @db.Timestamp(0)
  is_active                                    Boolean                     @default(true)
  created_by                                   BigInt?                     @db.UnsignedBigInt
  updated_by                                   BigInt?                     @db.UnsignedBigInt
  users_bulk_order_enquiries_created_byTousers User?                       @relation("bulk_order_enquiries_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_bulk_order_enquiries_created_by")
  users_bulk_order_enquiries_updated_byTousers User?                       @relation("bulk_order_enquiries_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_bulk_order_enquiries_updated_by")

  @@index([created_by], map: "fk_bulk_order_enquiries_created_by")
  @@index([updated_by], map: "fk_bulk_order_enquiries_updated_by")
}

model contact_messages {
  id                                       BigInt                  @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                     String                  @unique(map: "idx_contact_messages_uuid") @default("UUID()") @db.VarChar(255)
  name                                     String                  @db.VarChar(150)
  email                                    String                  @db.VarChar(150)
  phone                                    String?                 @db.VarChar(20)
  subject                                  String?                 @db.VarChar(200)
  message                                  String                  @db.Text
  status                                   contact_messages_status @default(new)
  created_at                               DateTime                @default(now()) @db.Timestamp(0)
  updated_at                               DateTime                @default(now()) @updatedAt @db.Timestamp(0)
  is_active                                Boolean                 @default(true)
  created_by                               BigInt?                 @db.UnsignedBigInt
  updated_by                               BigInt?                 @db.UnsignedBigInt
  users_contact_messages_created_byTousers User?                   @relation("contact_messages_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_contact_messages_created_by")
  users_contact_messages_updated_byTousers User?                   @relation("contact_messages_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_contact_messages_updated_by")

  @@index([created_by], map: "fk_contact_messages_created_by")
  @@index([updated_by], map: "fk_contact_messages_updated_by")
}

model coupon_usage {
  id                                   BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                 String?  @default("UUID()") @db.VarChar(255)
  coupon_id                            BigInt   @db.UnsignedBigInt
  user_id                              BigInt   @db.UnsignedBigInt
  order_id                             BigInt   @db.UnsignedBigInt
  discount_amount                      Decimal  @db.Decimal(10, 2)
  used_at                              DateTime @default(now()) @db.Timestamp(0)
  created_at                           DateTime @default(now()) @db.Timestamp(0)
  updated_at                           DateTime @default(now()) @db.Timestamp(0)
  is_active                            Boolean  @default(true)
  created_by                           BigInt?  @db.UnsignedBigInt
  updated_by                           BigInt?  @db.UnsignedBigInt
  users_coupon_usage_created_byTousers User?    @relation("coupon_usage_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_coupon_usage_created_by")
  users_coupon_usage_updated_byTousers User?    @relation("coupon_usage_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_coupon_usage_updated_by")
  coupons                              Coupon   @relation(fields: [coupon_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_couponusage_coupon")
  orders                               Order    @relation(fields: [order_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_couponusage_order")
  users_coupon_usage_user_idTousers    User     @relation("coupon_usage_user_idTousers", fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_couponusage_user")

  @@index([created_by], map: "fk_coupon_usage_created_by")
  @@index([updated_by], map: "fk_coupon_usage_updated_by")
  @@index([coupon_id], map: "fk_couponusage_coupon")
  @@index([order_id], map: "fk_couponusage_order")
  @@index([user_id], map: "fk_couponusage_user")
}

model customer_profiles {
  id                                         BigInt                    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                       String?                   @default("UUID()") @db.VarChar(255)
  user_id                                    BigInt                    @unique(map: "user_id") @db.UnsignedBigInt
  name                                       String?                   @db.VarChar(255)
  email                                      String?                   @db.VarChar(255)
  phone                                      String?                   @db.VarChar(13)
  is_whatsapp                                Boolean?                  @default(false)
  whatsapp_no                                String?                   @db.VarChar(13)
  dob                                        DateTime?                 @db.Date
  gender                                     customer_profiles_gender?
  profile_image                              String?                   @db.LongText
  referral_code                              String?                   @unique(map: "referral_code") @db.VarChar(30)
  referred_by                                BigInt?                   @db.UnsignedBigInt
  created_at                                 DateTime                  @default(now()) @db.Timestamp(0)
  updated_at                                 DateTime                  @default(now()) @db.Timestamp(0)
  is_active                                  Boolean                   @default(true)
  status                                     Boolean?                  @default(true)
  created_by                                 BigInt?                   @db.UnsignedBigInt
  updated_by                                 BigInt?                   @db.UnsignedBigInt
  users_customer_profiles_referred_byTousers User?                     @relation("customer_profiles_referred_byTousers", fields: [referred_by], references: [id], onUpdate: NoAction, map: "fk_cp_referrer")
  users_customer_profiles_user_idTousers     User                      @relation("customer_profiles_user_idTousers", fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_cp_user")
  users_customer_profiles_created_byTousers  User?                     @relation("customer_profiles_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_customer_profiles_created_by")
  users_customer_profiles_updated_byTousers  User?                     @relation("customer_profiles_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_customer_profiles_updated_by")

  @@index([referred_by], map: "fk_cp_referrer")
  @@index([created_by], map: "fk_customer_profiles_created_by")
  @@index([updated_by], map: "fk_customer_profiles_updated_by")
}

model customer_reports {
  id                                       BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  report_date                              DateTime @unique(map: "report_date") @db.Date
  new_customers                            Int      @default(0)
  returning_customers                      Int      @default(0)
  generated_at                             DateTime @default(now()) @db.Timestamp(0)
  created_at                               DateTime @default(now()) @db.Timestamp(0)
  updated_at                               DateTime @default(now()) @db.Timestamp(0)
  is_active                                Boolean  @default(true)
  created_by                               BigInt?  @db.UnsignedBigInt
  updated_by                               BigInt?  @db.UnsignedBigInt
  users_customer_reports_created_byTousers User?    @relation("customer_reports_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_customer_reports_created_by")
  users_customer_reports_updated_byTousers User?    @relation("customer_reports_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_customer_reports_updated_by")

  @@index([created_by], map: "fk_customer_reports_created_by")
  @@index([updated_by], map: "fk_customer_reports_updated_by")
}

model delivery_partners {
  id                                        BigInt      @id @default(autoincrement()) @db.UnsignedBigInt
  name                                      String      @db.VarChar(150)
  code                                      String      @unique(map: "code") @db.VarChar(50)
  contact_number                            String?     @db.VarChar(20)
  is_active                                 Boolean     @default(true)
  created_at                                DateTime    @default(now()) @db.Timestamp(0)
  updated_at                                DateTime    @default(now()) @db.Timestamp(0)
  created_by                                BigInt?     @db.UnsignedBigInt
  updated_by                                BigInt?     @db.UnsignedBigInt
  users_delivery_partners_created_byTousers User?       @relation("delivery_partners_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_delivery_partners_created_by")
  users_delivery_partners_updated_byTousers User?       @relation("delivery_partners_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_delivery_partners_updated_by")
  shipments                                 shipments[]

  @@index([created_by], map: "fk_delivery_partners_created_by")
  @@index([updated_by], map: "fk_delivery_partners_updated_by")
}

model delivery_slots {
  id                                     BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                   String?  @default("UUID()") @db.VarChar(255)
  slot_date                              DateTime @db.Date
  start_time                             DateTime @db.Time(0)
  end_time                               DateTime @db.Time(0)
  max_orders                             Int      @default(0)
  booked_orders                          Int      @default(0)
  is_active                              Boolean  @default(true)
  created_at                             DateTime @default(now()) @db.Timestamp(0)
  updated_at                             DateTime @default(now()) @db.Timestamp(0)
  created_by                             BigInt?  @db.UnsignedBigInt
  updated_by                             BigInt?  @db.UnsignedBigInt
  users_delivery_slots_created_byTousers User?    @relation("delivery_slots_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_delivery_slots_created_by")
  users_delivery_slots_updated_byTousers User?    @relation("delivery_slots_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_delivery_slots_updated_by")
  orders                                 Order[]

  @@index([created_by], map: "fk_delivery_slots_created_by")
  @@index([updated_by], map: "fk_delivery_slots_updated_by")
  @@index([slot_date], map: "idx_slot_date")
}

model faq {
  id                          BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  category                    String?  @db.VarChar(100)
  icon                        String?  @db.VarChar(50)
  question                    String   @db.VarChar(255)
  answer                      String   @db.Text
  sort_order                  Int      @default(0)
  is_active                   Boolean  @default(true)
  created_at                  DateTime @default(now()) @db.Timestamp(0)
  updated_at                  DateTime @default(now()) @db.Timestamp(0)
  created_by                  BigInt?  @db.UnsignedBigInt
  updated_by                  BigInt?  @db.UnsignedBigInt
  users_faq_created_byTousers User?    @relation("faq_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_faq_created_by")
  users_faq_updated_byTousers User?    @relation("faq_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_faq_updated_by")

  @@index([created_by], map: "fk_faq_created_by")
  @@index([updated_by], map: "fk_faq_updated_by")
  @@index([is_active, sort_order], map: "idx_faq_active_sort")
  @@index([category], map: "idx_faq_category")
}

model login_logs {
  id                                 BigInt            @id @default(autoincrement()) @db.UnsignedBigInt
  user_id                            BigInt?           @db.UnsignedBigInt
  ip_address                         String?           @db.VarChar(45)
  user_agent                         String?           @db.VarChar(255)
  status                             login_logs_status
  created_at                         DateTime          @default(now()) @db.Timestamp(0)
  updated_at                         DateTime          @default(now()) @db.Timestamp(0)
  is_active                          Boolean           @default(true)
  created_by                         BigInt?           @db.UnsignedBigInt
  updated_by                         BigInt?           @db.UnsignedBigInt
  users_login_logs_created_byTousers User?             @relation("login_logs_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_login_logs_created_by")
  users_login_logs_updated_byTousers User?             @relation("login_logs_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_login_logs_updated_by")
  users_login_logs_user_idTousers    User?             @relation("login_logs_user_idTousers", fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_loginlog_user")

  @@index([created_by], map: "fk_login_logs_created_by")
  @@index([updated_by], map: "fk_login_logs_updated_by")
  @@index([user_id], map: "fk_loginlog_user")
}

model newsletter_subscribers {
  id                                             BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  email                                          String   @unique(map: "email") @db.VarChar(150)
  is_active                                      Boolean  @default(true)
  subscribed_at                                  DateTime @default(now()) @db.Timestamp(0)
  created_at                                     DateTime @default(now()) @db.Timestamp(0)
  updated_at                                     DateTime @default(now()) @db.Timestamp(0)
  created_by                                     BigInt?  @db.UnsignedBigInt
  updated_by                                     BigInt?  @db.UnsignedBigInt
  users_newsletter_subscribers_created_byTousers User?    @relation("newsletter_subscribers_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_newsletter_subscribers_created_by")
  users_newsletter_subscribers_updated_byTousers User?    @relation("newsletter_subscribers_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_newsletter_subscribers_updated_by")

  @@index([created_by], map: "fk_newsletter_subscribers_created_by")
  @@index([updated_by], map: "fk_newsletter_subscribers_updated_by")
}

model notification_logs {
  id                                        BigInt                    @id @default(autoincrement()) @db.UnsignedBigInt
  notification_template_id                  BigInt?                   @db.UnsignedBigInt
  recipient                                 String                    @db.VarChar(150)
  channel                                   notification_logs_channel
  status                                    notification_logs_status  @default(pending)
  response                                  String?                   @db.Text
  sent_at                                   DateTime?                 @db.Timestamp(0)
  created_at                                DateTime                  @default(now()) @db.Timestamp(0)
  updated_at                                DateTime                  @default(now()) @db.Timestamp(0)
  is_active                                 Boolean                   @default(true)
  created_by                                BigInt?                   @db.UnsignedBigInt
  updated_by                                BigInt?                   @db.UnsignedBigInt
  users_notification_logs_created_byTousers User?                     @relation("notification_logs_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_notification_logs_created_by")
  users_notification_logs_updated_byTousers User?                     @relation("notification_logs_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_notification_logs_updated_by")
  notification_templates                    notification_templates?   @relation(fields: [notification_template_id], references: [id], onUpdate: NoAction, map: "fk_notiflog_template")

  @@index([created_by], map: "fk_notification_logs_created_by")
  @@index([updated_by], map: "fk_notification_logs_updated_by")
  @@index([notification_template_id], map: "fk_notiflog_template")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model notification_templates {
  id                                             BigInt                         @id @default(autoincrement()) @db.UnsignedBigInt
  code                                           String                         @unique(map: "code") @db.VarChar(100)
  channel                                        notification_templates_channel
  subject                                        String?                        @db.VarChar(255)
  body                                           String                         @db.Text
  variables                                      Json?
  is_active                                      Boolean                        @default(true)
  created_at                                     DateTime                       @default(now()) @db.Timestamp(0)
  updated_at                                     DateTime                       @default(now()) @db.Timestamp(0)
  created_by                                     BigInt?                        @db.UnsignedBigInt
  updated_by                                     BigInt?                        @db.UnsignedBigInt
  notification_logs                              notification_logs[]
  users_notification_templates_created_byTousers User?                          @relation("notification_templates_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_notification_templates_created_by")
  users_notification_templates_updated_byTousers User?                          @relation("notification_templates_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_notification_templates_updated_by")

  @@index([created_by], map: "fk_notification_templates_created_by")
  @@index([updated_by], map: "fk_notification_templates_updated_by")
}

model offer_products {
  id                                     BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                   String?  @default("UUID()") @db.VarChar(255)
  offer_id                               BigInt   @db.UnsignedBigInt
  product_id                             BigInt   @db.UnsignedBigInt
  created_at                             DateTime @default(now()) @db.Timestamp(0)
  updated_at                             DateTime @default(now()) @db.Timestamp(0)
  is_active                              Boolean  @default(true)
  created_by                             BigInt?  @db.UnsignedBigInt
  updated_by                             BigInt?  @db.UnsignedBigInt
  users_offer_products_created_byTousers User?    @relation("offer_products_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_offer_products_created_by")
  users_offer_products_updated_byTousers User?    @relation("offer_products_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_offer_products_updated_by")
  offers                                 Offer    @relation(fields: [offer_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_offerprod_offer")
  products                               Product  @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_offerprod_product")

  @@unique([offer_id, product_id], map: "uq_offer_product")
  @@index([created_by], map: "fk_offer_products_created_by")
  @@index([updated_by], map: "fk_offer_products_updated_by")
  @@index([product_id], map: "fk_offerprod_product")
}

model offer_items {
  id                                  BigInt           @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                String?          @default("UUID()") @db.VarChar(255)
  offer_id                            BigInt           @db.UnsignedBigInt
  variant_unit_price_id               BigInt           @db.UnsignedBigInt
  created_at                          DateTime         @default(now()) @db.Timestamp(0)
  updated_at                          DateTime         @default(now()) @db.Timestamp(0)
  is_active                           Boolean          @default(true)
  created_by                          BigInt?          @db.UnsignedBigInt
  updated_by                          BigInt?          @db.UnsignedBigInt
  users_offer_items_created_byTousers User?            @relation("offer_items_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_offer_items_created_by")
  users_offer_items_updated_byTousers User?            @relation("offer_items_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_offer_items_updated_by")
  offers                              Offer            @relation(fields: [offer_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_offeritem_offer")
  variant_unit_price                  VariantUnitPrice @relation(fields: [variant_unit_price_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_offeritem_vup")

  @@unique([offer_id, variant_unit_price_id], map: "uq_offer_item")
  @@index([created_by], map: "fk_offer_items_created_by")
  @@index([updated_by], map: "fk_offer_items_updated_by")
  @@index([variant_unit_price_id], map: "fk_offeritem_vup")
}

model order_status_history {
  id                                           BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  order_id                                     BigInt   @db.UnsignedBigInt
  status                                       String   @db.VarChar(50)
  note                                         String?  @db.VarChar(255)
  changed_by                                   BigInt?  @db.UnsignedBigInt
  created_at                                   DateTime @default(now()) @db.Timestamp(0)
  updated_at                                   DateTime @default(now()) @db.Timestamp(0)
  is_active                                    Boolean  @default(true)
  created_by                                   BigInt?  @db.UnsignedBigInt
  updated_by                                   BigInt?  @db.UnsignedBigInt
  users_order_status_history_created_byTousers User?    @relation("order_status_history_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_order_status_history_created_by")
  users_order_status_history_updated_byTousers User?    @relation("order_status_history_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_order_status_history_updated_by")
  orders                                       Order    @relation(fields: [order_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_orderhist_order")
  users_order_status_history_changed_byTousers User?    @relation("order_status_history_changed_byTousers", fields: [changed_by], references: [id], onUpdate: NoAction, map: "fk_orderhist_user")

  @@index([created_by], map: "fk_order_status_history_created_by")
  @@index([updated_by], map: "fk_order_status_history_updated_by")
  @@index([order_id], map: "fk_orderhist_order")
  @@index([changed_by], map: "fk_orderhist_user")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model otp_verifications {
  id                                        BigInt                    @id @default(autoincrement()) @db.UnsignedBigInt
  user_id                                   BigInt?                   @db.UnsignedBigInt
  identifier                                String                    @db.VarChar(150)
  otp_code                                  String                    @db.VarChar(64)
  purpose                                   otp_verifications_purpose
  is_used                                   Boolean                   @default(false)
  expires_at                                DateTime                  @db.Timestamp(0)
  verification_token_hash                   String?                   @db.VarChar(255)
  token_expires_at                          DateTime?                 @db.Timestamp(0)
  is_token_used                             Boolean                   @default(false)
  created_at                                DateTime                  @default(now()) @db.Timestamp(0)
  updated_at                                DateTime                  @default(now()) @db.Timestamp(0)
  is_active                                 Boolean                   @default(true)
  created_by                                BigInt?                   @db.UnsignedBigInt
  updated_by                                BigInt?                   @db.UnsignedBigInt
  users_otp_verifications_user_idTousers    User?                     @relation("otp_verifications_user_idTousers", fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_otp_user")
  users_otp_verifications_created_byTousers User?                     @relation("otp_verifications_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_otp_verifications_created_by")
  users_otp_verifications_updated_byTousers User?                     @relation("otp_verifications_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_otp_verifications_updated_by")

  @@index([user_id], map: "fk_otp_user")
  @@index([created_by], map: "fk_otp_verifications_created_by")
  @@index([updated_by], map: "fk_otp_verifications_updated_by")
  @@index([identifier], map: "idx_otp_identifier")
}

model page_contents {
  id                                    BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  page_id                               BigInt   @db.UnsignedBigInt
  content                               String   @db.LongText
  created_at                            DateTime @default(now()) @db.Timestamp(0)
  updated_at                            DateTime @default(now()) @db.Timestamp(0)
  is_active                             Boolean  @default(true)
  created_by                            BigInt?  @db.UnsignedBigInt
  updated_by                            BigInt?  @db.UnsignedBigInt
  users_page_contents_created_byTousers User?    @relation("page_contents_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_page_contents_created_by")
  users_page_contents_updated_byTousers User?    @relation("page_contents_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_page_contents_updated_by")
  pages                                 pages    @relation(fields: [page_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_pagecontent_page")

  @@index([created_by], map: "fk_page_contents_created_by")
  @@index([updated_by], map: "fk_page_contents_updated_by")
  @@index([page_id], map: "fk_pagecontent_page")
}

model pages {
  id                            BigInt          @id @default(autoincrement()) @db.UnsignedBigInt
  title                         String          @db.VarChar(200)
  slug                          String          @unique(map: "slug") @db.VarChar(220)
  is_active                     Boolean         @default(true)
  created_at                    DateTime        @default(now()) @db.Timestamp(0)
  updated_at                    DateTime        @default(now()) @db.Timestamp(0)
  created_by                    BigInt?         @db.UnsignedBigInt
  updated_by                    BigInt?         @db.UnsignedBigInt
  page_contents                 page_contents[]
  users_pages_created_byTousers User?           @relation("pages_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_pages_created_by")
  users_pages_updated_byTousers User?           @relation("pages_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_pages_updated_by")

  @@index([created_by], map: "fk_pages_created_by")
  @@index([updated_by], map: "fk_pages_updated_by")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model payment_gateway_webhooks {
  id                                               BigInt                          @id @default(autoincrement()) @db.UnsignedBigInt
  gateway                                          String                          @db.VarChar(50)
  event_type                                       String                          @db.VarChar(100)
  payload                                          Json
  status                                           payment_gateway_webhooks_status @default(received)
  received_at                                      DateTime                        @default(now()) @db.Timestamp(0)
  processed_at                                     DateTime?                       @db.Timestamp(0)
  created_at                                       DateTime                        @default(now()) @db.Timestamp(0)
  updated_at                                       DateTime                        @default(now()) @db.Timestamp(0)
  is_active                                        Boolean                         @default(true)
  created_by                                       BigInt?                         @db.UnsignedBigInt
  updated_by                                       BigInt?                         @db.UnsignedBigInt
  users_payment_gateway_webhooks_created_byTousers User?                           @relation("payment_gateway_webhooks_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_payment_gateway_webhooks_created_by")
  users_payment_gateway_webhooks_updated_byTousers User?                           @relation("payment_gateway_webhooks_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_payment_gateway_webhooks_updated_by")

  @@index([created_by], map: "fk_payment_gateway_webhooks_created_by")
  @@index([updated_by], map: "fk_payment_gateway_webhooks_updated_by")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model payment_methods {
  id                                      BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  name                                    String    @db.VarChar(100)
  code                                    String    @unique(map: "code") @db.VarChar(50)
  is_active                               Boolean   @default(true)
  created_at                              DateTime  @default(now()) @db.Timestamp(0)
  updated_at                              DateTime  @default(now()) @db.Timestamp(0)
  created_by                              BigInt?   @db.UnsignedBigInt
  updated_by                              BigInt?   @db.UnsignedBigInt
  users_payment_methods_created_byTousers User?     @relation("payment_methods_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_payment_methods_created_by")
  users_payment_methods_updated_byTousers User?     @relation("payment_methods_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_payment_methods_updated_by")
  payments                                Payment[]

  @@index([created_by], map: "fk_payment_methods_created_by")
  @@index([updated_by], map: "fk_payment_methods_updated_by")
}

model pincode_serviceability {
  id                                             BigInt         @id @default(autoincrement()) @db.UnsignedBigInt
  zone_id                                        BigInt         @db.UnsignedBigInt
  pincode                                        String         @db.VarChar(10)
  is_active                                      Boolean        @default(true)
  created_at                                     DateTime       @default(now()) @db.Timestamp(0)
  updated_at                                     DateTime       @default(now()) @db.Timestamp(0)
  created_by                                     BigInt?        @db.UnsignedBigInt
  updated_by                                     BigInt?        @db.UnsignedBigInt
  users_pincode_serviceability_created_byTousers User?          @relation("pincode_serviceability_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_pincode_serviceability_created_by")
  users_pincode_serviceability_updated_byTousers User?          @relation("pincode_serviceability_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_pincode_serviceability_updated_by")
  shipping_zones                                 shipping_zones @relation(fields: [zone_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_pincode_zone")

  @@unique([zone_id, pincode], map: "uq_zone_pincode")
  @@index([created_by], map: "fk_pincode_serviceability_created_by")
  @@index([updated_by], map: "fk_pincode_serviceability_updated_by")
  @@index([pincode], map: "idx_pincode")
}

model product_attribute_values {
  id                                               BigInt           @id @default(autoincrement()) @db.UnsignedBigInt
  product_id                                       BigInt           @db.UnsignedBigInt
  attribute_id                                     BigInt           @db.UnsignedBigInt
  attribute_value_id                               BigInt           @db.UnsignedBigInt
  created_at                                       DateTime         @default(now()) @db.Timestamp(0)
  updated_at                                       DateTime         @default(now()) @db.Timestamp(0)
  is_active                                        Boolean          @default(true)
  created_by                                       BigInt?          @db.UnsignedBigInt
  updated_by                                       BigInt?          @db.UnsignedBigInt
  product_attributes                               ProductAttribute @relation(fields: [attribute_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_pav_attr")
  products                                         Product          @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_pav_product")
  attribute_values                                 AttributeValue   @relation(fields: [attribute_value_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_pav_value")
  users_product_attribute_values_created_byTousers User?            @relation("product_attribute_values_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_product_attribute_values_created_by")
  users_product_attribute_values_updated_byTousers User?            @relation("product_attribute_values_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_product_attribute_values_updated_by")

  @@index([attribute_id], map: "fk_pav_attr")
  @@index([product_id], map: "fk_pav_product")
  @@index([attribute_value_id], map: "fk_pav_value")
  @@index([created_by], map: "fk_product_attribute_values_created_by")
  @@index([updated_by], map: "fk_product_attribute_values_updated_by")
}

model product_category_images {
  id                                              BigInt          @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                            String?         @default("UUID()") @db.VarChar(255)
  category_id                                     BigInt          @db.UnsignedBigInt
  image_url                                       String          @db.VarChar(500)
  alt_text                                        String?         @db.VarChar(150)
  sort_order                                      Int             @default(0)
  created_at                                      DateTime        @default(now()) @db.Timestamp(0)
  updated_at                                      DateTime        @default(now()) @db.Timestamp(0)
  status                                          Boolean?        @default(true)
  is_active                                       Boolean         @default(true)
  created_by                                      BigInt?         @db.UnsignedBigInt
  updated_by                                      BigInt?         @db.UnsignedBigInt
  users_product_category_images_created_byTousers User?           @relation("product_category_images_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_category_images_created_by")
  users_product_category_images_updated_byTousers User?           @relation("product_category_images_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_category_images_updated_by")
  product_categories                              ProductCategory @relation(fields: [category_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_catimg_cat")

  @@index([created_by], map: "fk_category_images_created_by")
  @@index([updated_by], map: "fk_category_images_updated_by")
  @@index([category_id], map: "fk_catimg_cat")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model product_gst_rates {
  id                                        BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                      String?             @default("UUID()") @db.VarChar(255)
  name                                      String              @db.VarChar(100)
  cgst_percent                              Decimal             @default(0.00) @db.Decimal(5, 2)
  sgst_percent                              Decimal             @default(0.00) @db.Decimal(5, 2)
  igst_percent                              Decimal             @default(0.00) @db.Decimal(5, 2)
  status                                    Boolean?            @default(true)
  is_active                                 Boolean             @default(true)
  created_at                                DateTime            @default(now()) @db.Timestamp(0)
  updated_at                                DateTime            @default(now()) @db.Timestamp(0)
  created_by                                BigInt?             @db.UnsignedBigInt
  updated_by                                BigInt?             @db.UnsignedBigInt
  users_product_gst_rates_created_byTousers User?               @relation("product_gst_rates_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_gst_rates_created_by")
  users_product_gst_rates_updated_byTousers User?               @relation("product_gst_rates_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_gst_rates_updated_by")
  product_hsn_codes                         product_hsn_codes[]

  @@index([created_by], map: "fk_gst_rates_created_by")
  @@index([updated_by], map: "fk_gst_rates_updated_by")
}

model product_hsn_codes {
  id                                        BigInt             @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                      String?            @default("UUID()") @db.VarChar(255)
  code                                      String             @unique(map: "code") @db.VarChar(20)
  description                               String?            @db.VarChar(255)
  gst_rate_id                               BigInt?            @db.UnsignedBigInt
  created_at                                DateTime           @default(now()) @db.Timestamp(0)
  updated_at                                DateTime           @default(now()) @db.Timestamp(0)
  status                                    Boolean?           @default(true)
  is_active                                 Boolean            @default(true)
  created_by                                BigInt?            @db.UnsignedBigInt
  updated_by                                BigInt?            @db.UnsignedBigInt
  users_product_hsn_codes_created_byTousers User?              @relation("product_hsn_codes_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_hsn_codes_created_by")
  users_product_hsn_codes_updated_byTousers User?              @relation("product_hsn_codes_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_hsn_codes_updated_by")
  product_gst_rates                         product_gst_rates? @relation(fields: [gst_rate_id], references: [id], onUpdate: NoAction, map: "fk_hsn_gst")
  products                                  Product[]

  @@index([created_by], map: "fk_hsn_codes_created_by")
  @@index([updated_by], map: "fk_hsn_codes_updated_by")
  @@index([gst_rate_id], map: "fk_hsn_gst")
}

model product_questions {
  id                                         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  product_id                                 BigInt    @db.UnsignedBigInt
  user_id                                    BigInt?   @db.UnsignedBigInt
  question                                   String    @db.VarChar(500)
  answer                                     String?   @db.VarChar(1000)
  answered_by                                BigInt?   @db.UnsignedBigInt
  is_published                               Boolean   @default(false)
  created_at                                 DateTime  @default(now()) @db.Timestamp(0)
  answered_at                                DateTime? @db.Timestamp(0)
  updated_at                                 DateTime  @default(now()) @db.Timestamp(0)
  is_active                                  Boolean   @default(true)
  created_by                                 BigInt?   @db.UnsignedBigInt
  updated_by                                 BigInt?   @db.UnsignedBigInt
  users_product_questions_answered_byTousers User?     @relation("product_questions_answered_byTousers", fields: [answered_by], references: [id], onUpdate: NoAction, map: "fk_pq_answered_by")
  products                                   Product   @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_pq_product")
  users_product_questions_user_idTousers     User?     @relation("product_questions_user_idTousers", fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_pq_user")
  users_product_questions_created_byTousers  User?     @relation("product_questions_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_product_questions_created_by")
  users_product_questions_updated_byTousers  User?     @relation("product_questions_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_product_questions_updated_by")

  @@index([answered_by], map: "fk_pq_answered_by")
  @@index([product_id], map: "fk_pq_product")
  @@index([user_id], map: "fk_pq_user")
  @@index([created_by], map: "fk_product_questions_created_by")
  @@index([updated_by], map: "fk_product_questions_updated_by")
}

model product_tag_maps {
  id                                       BigInt       @id @default(autoincrement()) @db.UnsignedBigInt
  product_id                               BigInt       @db.UnsignedBigInt
  tag_id                                   BigInt       @db.UnsignedBigInt
  created_at                               DateTime     @default(now()) @db.Timestamp(0)
  updated_at                               DateTime     @default(now()) @db.Timestamp(0)
  is_active                                Boolean      @default(true)
  created_by                               BigInt?      @db.UnsignedBigInt
  updated_by                               BigInt?      @db.UnsignedBigInt
  users_product_tag_maps_created_byTousers User?        @relation("product_tag_maps_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_product_tag_maps_created_by")
  users_product_tag_maps_updated_byTousers User?        @relation("product_tag_maps_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_product_tag_maps_updated_by")
  products                                 Product      @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ptm_product")
  product_tags                             product_tags @relation(fields: [tag_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_ptm_tag")

  @@unique([product_id, tag_id], map: "uq_product_tag")
  @@index([created_by], map: "fk_product_tag_maps_created_by")
  @@index([updated_by], map: "fk_product_tag_maps_updated_by")
  @@index([tag_id], map: "fk_ptm_tag")
}

model product_tags {
  id                                   BigInt             @id @default(autoincrement()) @db.UnsignedBigInt
  name                                 String             @db.VarChar(100)
  slug                                 String             @unique(map: "slug") @db.VarChar(120)
  created_at                           DateTime           @default(now()) @db.Timestamp(0)
  updated_at                           DateTime           @default(now()) @db.Timestamp(0)
  is_active                            Boolean            @default(true)
  created_by                           BigInt?            @db.UnsignedBigInt
  updated_by                           BigInt?            @db.UnsignedBigInt
  product_tag_maps                     product_tag_maps[]
  users_product_tags_created_byTousers User?              @relation("product_tags_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_product_tags_created_by")
  users_product_tags_updated_byTousers User?              @relation("product_tags_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_product_tags_updated_by")

  @@index([created_by], map: "fk_product_tags_created_by")
  @@index([updated_by], map: "fk_product_tags_updated_by")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model product_units {
  id                                    BigInt             @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                  String?            @default("UUID()") @db.VarChar(255)
  name                                  String             @db.VarChar(50)
  code                                  String             @unique(map: "code") @db.VarChar(10)
  type                                  product_units_type @default(count)
  base_unit_id                          BigInt?            @db.UnsignedBigInt
  conversion_factor                     Decimal            @default(1.0000) @db.Decimal(12, 4)
  status                                Boolean?           @default(true)
  is_active                             Boolean            @default(true)
  sort_order                            Int                @default(0)
  created_at                            DateTime           @default(now()) @db.Timestamp(0)
  updated_at                            DateTime           @default(now()) @db.Timestamp(0)
  created_by                            BigInt?            @db.UnsignedBigInt
  updated_by                            BigInt?            @db.UnsignedBigInt
  product_units                         product_units?     @relation("product_unitsToproduct_units", fields: [base_unit_id], references: [id], onUpdate: NoAction, map: "fk_unit_base")
  other_product_units                   product_units[]    @relation("product_unitsToproduct_units")
  users_product_units_created_byTousers User?              @relation("product_units_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_units_created_by")
  users_product_units_updated_byTousers User?              @relation("product_units_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_units_updated_by")
  variant_unit_prices                   VariantUnitPrice[]

  @@index([base_unit_id], map: "fk_unit_base")
  @@index([created_by], map: "fk_units_created_by")
  @@index([updated_by], map: "fk_units_updated_by")
}

model product_variant_images {
  id                                             BigInt         @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                           String?        @db.VarChar(255)
  variant_id                                     BigInt         @db.UnsignedBigInt
  image_url                                      String         @db.VarChar(500)
  sort_order                                     Int            @default(0)
  is_primary                                     Boolean        @default(false) @map("is_primary")
  created_at                                     DateTime       @default(now()) @db.Timestamp(0)
  updated_at                                     DateTime       @default(now()) @db.Timestamp(0)
  status                                         Boolean?       @default(true)
  is_active                                      Boolean        @default(true)
  created_by                                     BigInt?        @db.UnsignedBigInt
  updated_by                                     BigInt?        @db.UnsignedBigInt
  users_product_variant_images_created_byTousers User?          @relation("product_variant_images_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_product_variant_images_created_by")
  users_product_variant_images_updated_byTousers User?          @relation("product_variant_images_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_product_variant_images_updated_by")
  product_variants                               ProductVariant @relation(fields: [variant_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_vimg_variant")

  @@index([created_by], map: "fk_product_variant_images_created_by")
  @@index([updated_by], map: "fk_product_variant_images_updated_by")
  @@index([variant_id], map: "fk_vimg_variant")
}

model produt_brand_images {
  id                                          BigInt       @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                        String?      @default("UUID()") @db.VarChar(255)
  brand_id                                    BigInt       @db.UnsignedBigInt
  image_url                                   String       @db.VarChar(500)
  alt_text                                    String?      @db.VarChar(150)
  created_at                                  DateTime     @default(now()) @db.Timestamp(0)
  updated_at                                  DateTime     @default(now()) @db.Timestamp(0)
  status                                      Boolean?     @default(true)
  is_active                                   Boolean      @default(true)
  created_by                                  BigInt?      @db.UnsignedBigInt
  updated_by                                  BigInt?      @db.UnsignedBigInt
  users_produt_brand_images_created_byTousers User?        @relation("produt_brand_images_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_brand_images_created_by")
  users_produt_brand_images_updated_byTousers User?        @relation("produt_brand_images_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_brand_images_updated_by")
  product_brands                              ProductBrand @relation(fields: [brand_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_brandimg_brand")

  @@index([created_by], map: "fk_brand_images_created_by")
  @@index([updated_by], map: "fk_brand_images_updated_by")
  @@index([brand_id], map: "fk_brandimg_brand")
}

model recently_viewed_products {
  id                                               BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  user_id                                          BigInt?  @db.UnsignedBigInt
  session_id                                       String?  @db.VarChar(150)
  product_id                                       BigInt   @db.UnsignedBigInt
  viewed_at                                        DateTime @default(now()) @db.Timestamp(0)
  created_at                                       DateTime @default(now()) @db.Timestamp(0)
  updated_at                                       DateTime @default(now()) @db.Timestamp(0)
  is_active                                        Boolean  @default(true)
  created_by                                       BigInt?  @db.UnsignedBigInt
  updated_by                                       BigInt?  @db.UnsignedBigInt
  users_recently_viewed_products_created_byTousers User?    @relation("recently_viewed_products_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_recently_viewed_products_created_by")
  users_recently_viewed_products_updated_byTousers User?    @relation("recently_viewed_products_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_recently_viewed_products_updated_by")
  products                                         Product  @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_recentview_product")
  users_recently_viewed_products_user_idTousers    User?    @relation("recently_viewed_products_user_idTousers", fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_recentview_user")

  @@index([created_by], map: "fk_recently_viewed_products_created_by")
  @@index([updated_by], map: "fk_recently_viewed_products_updated_by")
  @@index([product_id], map: "fk_recentview_product")
  @@index([session_id], map: "idx_recentview_session")
  @@index([user_id], map: "idx_recentview_user")
}

model redirect_urls {
  id                                    BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  from_url                              String   @db.VarChar(500)
  to_url                                String   @db.VarChar(500)
  redirect_type                         Int      @default(301) @db.SmallInt
  is_active                             Boolean  @default(true)
  created_at                            DateTime @default(now()) @db.Timestamp(0)
  updated_at                            DateTime @default(now()) @db.Timestamp(0)
  created_by                            BigInt?  @db.UnsignedBigInt
  updated_by                            BigInt?  @db.UnsignedBigInt
  users_redirect_urls_created_byTousers User?    @relation("redirect_urls_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_redirect_urls_created_by")
  users_redirect_urls_updated_byTousers User?    @relation("redirect_urls_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_redirect_urls_updated_by")

  @@index([created_by], map: "fk_redirect_urls_created_by")
  @@index([updated_by], map: "fk_redirect_urls_updated_by")
}

model refunds {
  id                              BigInt         @id @default(autoincrement()) @db.UnsignedBigInt
  payment_id                      BigInt         @db.UnsignedBigInt
  order_id                        BigInt         @db.UnsignedBigInt
  amount                          Decimal        @db.Decimal(10, 2)
  reason                          String?        @db.VarChar(255)
  status                          refunds_status @default(initiated)
  processed_at                    DateTime?      @db.Timestamp(0)
  created_at                      DateTime       @default(now()) @db.Timestamp(0)
  updated_at                      DateTime       @default(now()) @db.Timestamp(0)
  is_active                       Boolean        @default(true)
  created_by                      BigInt?        @db.UnsignedBigInt
  updated_by                      BigInt?        @db.UnsignedBigInt
  orders                          Order          @relation(fields: [order_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_refund_order")
  payments                        Payment        @relation(fields: [payment_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_refund_payment")
  users_refunds_created_byTousers User?          @relation("refunds_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_refunds_created_by")
  users_refunds_updated_byTousers User?          @relation("refunds_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_refunds_updated_by")

  @@index([order_id], map: "fk_refund_order")
  @@index([payment_id], map: "fk_refund_payment")
  @@index([created_by], map: "fk_refunds_created_by")
  @@index([updated_by], map: "fk_refunds_updated_by")
}

model return_items {
  id                                   BigInt          @id @default(autoincrement()) @db.UnsignedBigInt
  return_request_id                    BigInt          @db.UnsignedBigInt
  order_item_id                        BigInt          @db.UnsignedBigInt
  quantity                             Int
  reason                               String?         @db.VarChar(255)
  refund_amount                        Decimal?        @db.Decimal(10, 2)
  created_at                           DateTime        @default(now()) @db.Timestamp(0)
  updated_at                           DateTime        @default(now()) @db.Timestamp(0)
  is_active                            Boolean         @default(true)
  created_by                           BigInt?         @db.UnsignedBigInt
  updated_by                           BigInt?         @db.UnsignedBigInt
  users_return_items_created_byTousers User?           @relation("return_items_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_return_items_created_by")
  users_return_items_updated_byTousers User?           @relation("return_items_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_return_items_updated_by")
  order_items                          OrderItem       @relation(fields: [order_item_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_returnitem_orderitem")
  return_requests                      return_requests @relation(fields: [return_request_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_returnitem_request")

  @@index([created_by], map: "fk_return_items_created_by")
  @@index([updated_by], map: "fk_return_items_updated_by")
  @@index([order_item_id], map: "fk_returnitem_orderitem")
  @@index([return_request_id], map: "fk_returnitem_request")
}

model return_requests {
  id                                      BigInt                 @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                    String?                @unique(map: "idx_return_requests_uuid") @db.VarChar(255)
  order_id                                BigInt                 @db.UnsignedBigInt
  user_id                                 BigInt                 @db.UnsignedBigInt
  reason                                  String                 @db.VarChar(255)
  status                                  return_requests_status @default(requested)
  requested_at                            DateTime               @default(now()) @db.Timestamp(0)
  approved_at                             DateTime?              @db.Timestamp(0)
  created_at                              DateTime               @default(now()) @db.Timestamp(0)
  updated_at                              DateTime               @default(now()) @db.Timestamp(0)
  is_active                               Boolean                @default(true)
  created_by                              BigInt?                @db.UnsignedBigInt
  updated_by                              BigInt?                @db.UnsignedBigInt
  return_items                            return_items[]
  orders                                  Order                  @relation(fields: [order_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_return_order")
  users_return_requests_created_byTousers User?                  @relation("return_requests_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_return_requests_created_by")
  users_return_requests_updated_byTousers User?                  @relation("return_requests_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_return_requests_updated_by")
  users_return_requests_user_idTousers    User                   @relation("return_requests_user_idTousers", fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_return_user")

  @@index([order_id], map: "fk_return_order")
  @@index([created_by], map: "fk_return_requests_created_by")
  @@index([updated_by], map: "fk_return_requests_updated_by")
  @@index([user_id], map: "fk_return_user")
}

model reward_points {
  id                                    BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  user_id                               BigInt   @unique(map: "user_id") @db.UnsignedBigInt
  total_points                          Int      @default(0)
  created_at                            DateTime @default(now()) @db.Timestamp(0)
  updated_at                            DateTime @default(now()) @db.Timestamp(0)
  is_active                             Boolean  @default(true)
  created_by                            BigInt?  @db.UnsignedBigInt
  updated_by                            BigInt?  @db.UnsignedBigInt
  users_reward_points_created_byTousers User?    @relation("reward_points_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_reward_points_created_by")
  users_reward_points_updated_byTousers User?    @relation("reward_points_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_reward_points_updated_by")
  users_reward_points_user_idTousers    User     @relation("reward_points_user_idTousers", fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_rewardpoints_user")

  @@index([created_by], map: "fk_reward_points_created_by")
  @@index([updated_by], map: "fk_reward_points_updated_by")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model reward_transactions {
  id                                          BigInt                   @id @default(autoincrement()) @db.UnsignedBigInt
  user_id                                     BigInt                   @db.UnsignedBigInt
  type                                        reward_transactions_type
  points                                      Int
  reference_type                              String?                  @db.VarChar(50)
  reference_id                                BigInt?                  @db.UnsignedBigInt
  note                                        String?                  @db.VarChar(255)
  created_at                                  DateTime                 @default(now()) @db.Timestamp(0)
  updated_at                                  DateTime                 @default(now()) @db.Timestamp(0)
  is_active                                   Boolean                  @default(true)
  created_by                                  BigInt?                  @db.UnsignedBigInt
  updated_by                                  BigInt?                  @db.UnsignedBigInt
  users_reward_transactions_created_byTousers User?                    @relation("reward_transactions_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_reward_transactions_created_by")
  users_reward_transactions_updated_byTousers User?                    @relation("reward_transactions_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_reward_transactions_updated_by")
  users_reward_transactions_user_idTousers    User                     @relation("reward_transactions_user_idTousers", fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_rewardtx_user")

  @@index([created_by], map: "fk_reward_transactions_created_by")
  @@index([updated_by], map: "fk_reward_transactions_updated_by")
  @@index([user_id], map: "fk_rewardtx_user")
}

model sales_reports {
  id                                    BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  report_date                           DateTime @unique(map: "report_date") @db.Date
  total_orders                          Int      @default(0)
  total_sales                           Decimal  @default(0.00) @db.Decimal(12, 2)
  total_discount                        Decimal  @default(0.00) @db.Decimal(12, 2)
  total_tax                             Decimal  @default(0.00) @db.Decimal(12, 2)
  generated_at                          DateTime @default(now()) @db.Timestamp(0)
  created_at                            DateTime @default(now()) @db.Timestamp(0)
  updated_at                            DateTime @default(now()) @db.Timestamp(0)
  is_active                             Boolean  @default(true)
  created_by                            BigInt?  @db.UnsignedBigInt
  updated_by                            BigInt?  @db.UnsignedBigInt
  users_sales_reports_created_byTousers User?    @relation("sales_reports_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_sales_reports_created_by")
  users_sales_reports_updated_byTousers User?    @relation("sales_reports_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_sales_reports_updated_by")

  @@index([created_by], map: "fk_sales_reports_created_by")
  @@index([updated_by], map: "fk_sales_reports_updated_by")
}

model search_history {
  id                                     BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  user_id                                BigInt?  @db.UnsignedBigInt
  session_id                             String?  @db.VarChar(150)
  keyword                                String   @db.VarChar(255)
  results_count                          Int      @default(0)
  searched_at                            DateTime @default(now()) @db.Timestamp(0)
  created_at                             DateTime @default(now()) @db.Timestamp(0)
  updated_at                             DateTime @default(now()) @db.Timestamp(0)
  is_active                              Boolean  @default(true)
  created_by                             BigInt?  @db.UnsignedBigInt
  updated_by                             BigInt?  @db.UnsignedBigInt
  users_search_history_created_byTousers User?    @relation("search_history_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_search_history_created_by")
  users_search_history_updated_byTousers User?    @relation("search_history_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_search_history_updated_by")
  users_search_history_user_idTousers    User?    @relation("search_history_user_idTousers", fields: [user_id], references: [id], onUpdate: NoAction, map: "fk_search_user")

  @@index([created_by], map: "fk_search_history_created_by")
  @@index([updated_by], map: "fk_search_history_updated_by")
  @@index([user_id], map: "fk_search_user")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model seo_meta {
  id                               BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  entity_type                      String   @db.VarChar(50)
  entity_id                        BigInt   @db.UnsignedBigInt
  meta_title                       String?  @db.VarChar(200)
  meta_description                 String?  @db.VarChar(500)
  meta_keywords                    String?  @db.VarChar(255)
  og_image                         String?  @db.VarChar(500)
  created_at                       DateTime @default(now()) @db.Timestamp(0)
  updated_at                       DateTime @default(now()) @db.Timestamp(0)
  is_active                        Boolean  @default(true)
  created_by                       BigInt?  @db.UnsignedBigInt
  updated_by                       BigInt?  @db.UnsignedBigInt
  users_seo_meta_created_byTousers User?    @relation("seo_meta_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_seo_meta_created_by")
  users_seo_meta_updated_byTousers User?    @relation("seo_meta_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_seo_meta_updated_by")

  @@unique([entity_type, entity_id], map: "uq_seo_entity")
  @@index([created_by], map: "fk_seo_meta_created_by")
  @@index([updated_by], map: "fk_seo_meta_updated_by")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model settings {
  id                               BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  key_name                         String   @unique(map: "key_name") @db.VarChar(150)
  value                            String?  @db.Text
  type                             String   @default("string") @db.VarChar(50)
  created_at                       DateTime @default(now()) @db.Timestamp(0)
  updated_at                       DateTime @default(now()) @db.Timestamp(0)
  is_active                        Boolean  @default(true)
  created_by                       BigInt?  @db.UnsignedBigInt
  updated_by                       BigInt?  @db.UnsignedBigInt
  users_settings_created_byTousers User?    @relation("settings_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_settings_created_by")
  users_settings_updated_byTousers User?    @relation("settings_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_settings_updated_by")

  @@index([created_by], map: "fk_settings_created_by")
  @@index([updated_by], map: "fk_settings_updated_by")
}

model shipment_tracking {
  id                                        BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  shipment_id                               BigInt    @db.UnsignedBigInt
  status                                    String    @db.VarChar(100)
  location                                  String?   @db.VarChar(150)
  note                                      String?   @db.VarChar(255)
  tracked_at                                DateTime  @default(now()) @db.Timestamp(0)
  created_at                                DateTime  @default(now()) @db.Timestamp(0)
  updated_at                                DateTime  @default(now()) @db.Timestamp(0)
  is_active                                 Boolean   @default(true)
  created_by                                BigInt?   @db.UnsignedBigInt
  updated_by                                BigInt?   @db.UnsignedBigInt
  users_shipment_tracking_created_byTousers User?     @relation("shipment_tracking_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_shipment_tracking_created_by")
  users_shipment_tracking_updated_byTousers User?     @relation("shipment_tracking_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_shipment_tracking_updated_by")
  shipments                                 shipments @relation(fields: [shipment_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_shiptrack_shipment")

  @@index([created_by], map: "fk_shipment_tracking_created_by")
  @@index([updated_by], map: "fk_shipment_tracking_updated_by")
  @@index([shipment_id], map: "fk_shiptrack_shipment")
}

model shipments {
  id                                BigInt              @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                              String?             @db.VarChar(255)
  order_id                          BigInt              @db.UnsignedBigInt
  delivery_staff_id                 BigInt?             @db.UnsignedBigInt
  delivery_partner_id               BigInt?             @db.UnsignedBigInt
  tracking_number                   String?             @db.VarChar(150)
  status                            shipments_status    @default(pending)
  assignment_status                 String?             @default("pending") @db.VarChar(50)
  accepted_at                       DateTime?           @db.Timestamp(0)
  delivery_notes                    String?             @db.VarChar(500)
  shipped_at                        DateTime?           @db.Timestamp(0)
  delivered_at                      DateTime?           @db.Timestamp(0)
  created_at                        DateTime            @default(now()) @db.Timestamp(0)
  updated_at                        DateTime            @default(now()) @db.Timestamp(0)
  is_active                         Boolean             @default(true)
  created_by                        BigInt?             @db.UnsignedBigInt
  updated_by                        BigInt?             @db.UnsignedBigInt
  shipment_tracking                 shipment_tracking[]
  orders                            Order               @relation(fields: [order_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_shipment_order")
  delivery_partners                 delivery_partners?  @relation(fields: [delivery_partner_id], references: [id], onUpdate: NoAction, map: "fk_shipment_partner")
  delivery_staff                    User?               @relation("shipments_delivery_staffTousers", fields: [delivery_staff_id], references: [id], onUpdate: NoAction, map: "fk_shipment_delivery_staff")
  users_shipments_created_byTousers User?               @relation("shipments_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_shipments_created_by")
  users_shipments_updated_byTousers User?               @relation("shipments_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_shipments_updated_by")

  @@index([order_id], map: "fk_shipment_order")
  @@index([delivery_staff_id], map: "fk_shipment_delivery_staff")
  @@index([delivery_partner_id], map: "fk_shipment_partner")
  @@index([created_by], map: "fk_shipments_created_by")
  @@index([updated_by], map: "fk_shipments_updated_by")
}

model shipping_charges {
  id                                       BigInt          @id @default(autoincrement()) @db.UnsignedBigInt
  zone_id                                  BigInt?         @db.UnsignedBigInt
  min_order_amount                         Decimal         @default(0.00) @db.Decimal(10, 2)
  max_order_amount                         Decimal?        @db.Decimal(10, 2)
  charge_amount                            Decimal         @default(0.00) @db.Decimal(10, 2)
  is_free_above                            Boolean         @default(false)
  created_at                               DateTime        @default(now()) @db.Timestamp(0)
  updated_at                               DateTime        @default(now()) @db.Timestamp(0)
  is_active                                Boolean         @default(true)
  created_by                               BigInt?         @db.UnsignedBigInt
  updated_by                               BigInt?         @db.UnsignedBigInt
  shipping_zones                           shipping_zones? @relation(fields: [zone_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_shipcharge_zone")
  users_shipping_charges_created_byTousers User?           @relation("shipping_charges_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_shipping_charges_created_by")
  users_shipping_charges_updated_byTousers User?           @relation("shipping_charges_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_shipping_charges_updated_by")

  @@index([zone_id], map: "fk_shipcharge_zone")
  @@index([created_by], map: "fk_shipping_charges_created_by")
  @@index([updated_by], map: "fk_shipping_charges_updated_by")
}

model shipping_zones {
  id                                     BigInt                   @id @default(autoincrement()) @db.UnsignedBigInt
  name                                   String                   @db.VarChar(150)
  is_active                              Boolean                  @default(true)
  created_at                             DateTime                 @default(now()) @db.Timestamp(0)
  updated_at                             DateTime                 @default(now()) @db.Timestamp(0)
  created_by                             BigInt?                  @db.UnsignedBigInt
  updated_by                             BigInt?                  @db.UnsignedBigInt
  pincode_serviceability                 pincode_serviceability[]
  shipping_charges                       shipping_charges[]
  users_shipping_zones_created_byTousers User?                    @relation("shipping_zones_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_shipping_zones_created_by")
  users_shipping_zones_updated_byTousers User?                    @relation("shipping_zones_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_shipping_zones_updated_by")

  @@index([created_by], map: "fk_shipping_zones_created_by")
  @@index([updated_by], map: "fk_shipping_zones_updated_by")
}

model stock_adjustments {
  id                                         BigInt                            @id @default(autoincrement()) @db.UnsignedBigInt
  variant_unit_price_id                      BigInt                            @db.UnsignedBigInt
  adjusted_by                                BigInt?                           @db.UnsignedBigInt
  adjustment_type                            stock_adjustments_adjustment_type
  quantity                                   Int
  reason                                     String?                           @db.VarChar(255)
  created_at                                 DateTime                          @default(now()) @db.Timestamp(0)
  updated_at                                 DateTime                          @default(now()) @db.Timestamp(0)
  is_active                                  Boolean                           @default(true)
  created_by                                 BigInt?                           @db.UnsignedBigInt
  updated_by                                 BigInt?                           @db.UnsignedBigInt
  users_stock_adjustments_created_byTousers  User?                             @relation("stock_adjustments_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_stock_adjustments_created_by")
  users_stock_adjustments_updated_byTousers  User?                             @relation("stock_adjustments_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_stock_adjustments_updated_by")
  users_stock_adjustments_adjusted_byTousers User?                             @relation("stock_adjustments_adjusted_byTousers", fields: [adjusted_by], references: [id], onUpdate: NoAction, map: "fk_stockadj_user")
  variant_unit_price                         VariantUnitPrice                  @relation(fields: [variant_unit_price_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_stockadj_variant_unit_price")

  @@index([created_by], map: "fk_stock_adjustments_created_by")
  @@index([updated_by], map: "fk_stock_adjustments_updated_by")
  @@index([adjusted_by], map: "fk_stockadj_user")
  @@index([variant_unit_price_id], map: "fk_stockadj_variant_unit_price")
}

model stock_reports {
  id                                    BigInt           @id @default(autoincrement()) @db.UnsignedBigInt
  report_date                           DateTime         @db.Date
  variant_unit_price_id                 BigInt           @db.UnsignedBigInt
  opening_stock                         Int              @default(0)
  closing_stock                         Int              @default(0)
  sold_qty                              Int              @default(0)
  generated_at                          DateTime         @default(now()) @db.Timestamp(0)
  created_at                            DateTime         @default(now()) @db.Timestamp(0)
  updated_at                            DateTime         @default(now()) @db.Timestamp(0)
  is_active                             Boolean          @default(true)
  created_by                            BigInt?          @db.UnsignedBigInt
  updated_by                            BigInt?          @db.UnsignedBigInt
  users_stock_reports_created_byTousers User?            @relation("stock_reports_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_stock_reports_created_by")
  users_stock_reports_updated_byTousers User?            @relation("stock_reports_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_stock_reports_updated_by")
  variant_unit_price                    VariantUnitPrice @relation(fields: [variant_unit_price_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_stockreport_variant_unit_price")

  @@index([created_by], map: "fk_stock_reports_created_by")
  @@index([updated_by], map: "fk_stock_reports_updated_by")
  @@index([variant_unit_price_id], map: "fk_stockreport_variant_unit_price")
}

model user_sessions {
  id                                    BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  user_id                               BigInt   @db.UnsignedBigInt
  token                                 String   @unique(map: "token") @db.VarChar(255)
  ip_address                            String?  @db.VarChar(45)
  user_agent                            String?  @db.VarChar(255)
  device                                String?  @db.VarChar(100)
  expires_at                            DateTime @db.Timestamp(0)
  created_at                            DateTime @default(now()) @db.Timestamp(0)
  updated_at                            DateTime @default(now()) @db.Timestamp(0)
  is_active                             Boolean  @default(true)
  created_by                            BigInt?  @db.UnsignedBigInt
  updated_by                            BigInt?  @db.UnsignedBigInt
  users_user_sessions_user_idTousers    User     @relation("user_sessions_user_idTousers", fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_sessions_user")
  users_user_sessions_created_byTousers User?    @relation("user_sessions_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_user_sessions_created_by")
  users_user_sessions_updated_byTousers User?    @relation("user_sessions_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_user_sessions_updated_by")

  @@index([created_by], map: "fk_user_sessions_created_by")
  @@index([updated_by], map: "fk_user_sessions_updated_by")
  @@index([user_id], map: "idx_sessions_user")
}

enum banners_media_type {
  image
  video
}

enum inventory_transactions_type {
  in
  out
  reserved
  released
}

enum notification_templates_channel {
  email
  sms
  push
  whatsapp
}

enum payment_transactions_transaction_type {
  charge
  refund
}

enum reward_transactions_type {
  earn
  redeem
  expire
}

enum wallet_transactions_type {
  credit
  debit
}

enum coupons_type {
  flat
  percentage
}

enum notification_logs_channel {
  email
  sms
  push
  whatsapp
}

enum offers_type {
  percentage
  flat
  special_price
  bxgy
}

enum offers_level {
  product
  item
}

enum order_addresses_type {
  billing
  shipping
}

enum stock_adjustments_adjustment_type {
  add
  remove
}

enum audit_logs_action {
  create
  update
  delete
}

enum login_logs_status {
  success
  failed
}

enum notification_logs_status {
  sent
  failed
  pending
}

enum otp_verifications_purpose {
  login
  register
  reset_password
  checkout
}

enum payment_gateway_webhooks_status {
  received
  processed
  failed
}

enum product_units_type {
  weight
  volume
  count
  size
}

enum return_requests_status {
  requested
  approved
  rejected
  picked_up
  refunded
}

enum shipments_status {
  pending
  picked_up
  in_transit
  out_for_delivery
  delivered
  failed
}

enum carts_status {
  active
  converted
  abandoned
}

enum payments_status {
  pending
  success
  failed
  refunded
}

enum refunds_status {
  initiated
  processing
  completed
  failed
}

enum contact_messages_status {
  new
  read
  replied
}

enum bulk_order_enquiries_status {
  new
  contacted
  closed
}

enum orders_order_status {
  pending
  confirmed
  processing
  packed
  shipped
  out_for_delivery
  delivered
  cancelled
  returned
}

enum orders_payment_status {
  pending
  paid
  failed
  refunded
  partial_refund
}

enum customer_profiles_gender {
  male
  female
  other
}

enum users_status {
  active
  inactive
  banned
}

enum products_veg_type {
  veg
  nonveg
  vegan
  na
}

model variant_price_history {
  id                                            BigInt           @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                          String           @default("UUID()") @db.VarChar(255)
  variant_unit_price_id                         BigInt           @db.UnsignedBigInt
  old_base_price                                Decimal?         @db.Decimal(10, 2)
  new_base_price                                Decimal?         @db.Decimal(10, 2)
  changed_at                                    DateTime         @default(now()) @db.Timestamp(0)
  created_at                                    DateTime         @default(now()) @db.Timestamp(0)
  updated_at                                    DateTime         @default(now()) @updatedAt @db.Timestamp(0)
  is_active                                     Boolean          @default(true)
  created_by                                    BigInt?          @db.UnsignedBigInt
  updated_by                                    BigInt?          @db.UnsignedBigInt
  variant_unit_price                            VariantUnitPrice @relation(fields: [variant_unit_price_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_vph_variant_unit_price")
  users_variant_price_history_created_byTousers User?            @relation("variant_price_history_created_byTousers", fields: [created_by], references: [id], onUpdate: NoAction, map: "fk_vph_created_by")
  users_variant_price_history_updated_byTousers User?            @relation("variant_price_history_updated_byTousers", fields: [updated_by], references: [id], onUpdate: NoAction, map: "fk_vph_updated_by")

  @@index([variant_unit_price_id], map: "fk_vph_variant_unit_price")
  @@index([created_by], map: "fk_vph_created_by")
  @@index([updated_by], map: "fk_vph_updated_by")
  @@map("variant_price_history")
}

enum customer_addresses_address_type {
  shipping
  billing
}

model WhatsAppCampaign {
  id               BigInt                      @id @default(autoincrement()) @db.UnsignedBigInt
  uuid             String                      @default("UUID()") @db.VarChar(255)
  name             String                      @db.VarChar(255)
  description      String?                     @db.Text
  type             String                      @default("CUSTOM") @db.VarChar(100)
  status           String                      @default("DRAFT") @db.VarChar(50)
  message          String                      @db.Text
  media_url        String?                     @db.VarChar(500)
  scheduled_at     DateTime?                   @db.Timestamp(0)
  started_at       DateTime?                   @db.Timestamp(0)
  completed_at     DateTime?                   @db.Timestamp(0)
  total_recipients Int                         @default(0)
  sent_count       Int                         @default(0)
  failed_count     Int                         @default(0)
  created_at       DateTime                    @default(now()) @db.Timestamp(0)
  updated_at       DateTime                    @default(now()) @updatedAt @db.Timestamp(0)
  recipients       WhatsAppCampaignRecipient[]

  @@index([status], map: "idx_wac_status")
  @@map("whatsapp_campaigns")
}

model WhatsAppCampaignRecipient {
  id            BigInt           @id @default(autoincrement()) @db.UnsignedBigInt
  campaign_id   BigInt           @db.UnsignedBigInt
  customer_id   BigInt?          @db.UnsignedBigInt
  customer_name String?          @db.VarChar(255)
  phone_number  String           @db.VarChar(50)
  status        String           @default("QUEUED") @db.VarChar(50)
  error_message String?          @db.Text
  message_id    String?          @db.VarChar(100)
  sent_at       DateTime?        @db.Timestamp(0)
  created_at    DateTime         @default(now()) @db.Timestamp(0)
  updated_at    DateTime         @default(now()) @updatedAt @db.Timestamp(0)
  campaign      WhatsAppCampaign @relation(fields: [campaign_id], references: [id], onDelete: Cascade, map: "fk_wacr_campaign")

  @@unique([campaign_id, phone_number], map: "uq_wacr_camp_phone")
  @@index([campaign_id, status], map: "idx_wacr_camp_status")
  @@map("whatsapp_campaign_recipients")
}

model WhatsAppTemplate {
  id         BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  name       String   @db.VarChar(255)
  category   String   @default("FESTIVAL") @db.VarChar(100)
  message    String   @db.Text
  media_url  String?  @db.VarChar(500)
  is_active  Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamp(0)
  updated_at DateTime @default(now()) @updatedAt @db.Timestamp(0)

  @@map("whatsapp_templates")
}
